# Feature Verification Registry

Statuses: `NOT_STARTED` · `IN_PROGRESS` · `READY_FOR_TEST` · `FAILED` ·
`VERIFIED` · `DIRTY` · `BLOCKED`.

Only `VERIFIED` means the feature passed **real runtime acceptance** — real
browser, real API, real PostgreSQL (see `00-test-policy.md`).

Last run: 2026-08-24, stack on `localhost:5173` (Vite) → `localhost:5019` (API)
→ PostgreSQL 15 and MinIO in Docker.

> ⚠️ **The suite is not green.** The last full run was **51 passed, 30 failed**.
> Sixteen of those failures are specs still written against Ant Design selectors
> (`tr.ant-table-row`), and `antd` is no longer a dependency; three more are forms
> whose `<label>` carries no `htmlFor`. They fail in isolation too, so they are rot
> rather than interference. Do not read a run through `| tail -N`: the exit code
> then comes from `tail` and the failure list scrolls away — that is how earlier
> "N passed" numbers in this file were produced. Every `VERIFIED` row backed by one
> of the failing specs needs re-checking. See `save/taxonomy-handoff.md` §6.

| ID | Feature | Status | Acceptance spec | Notes |
|----|---------|--------|-----------------|-------|
| F-01 | Đăng nhập | `VERIFIED` | `e2e/fixtures/auth.ts` (used by every spec) | Real login form, real cookie auth |
| F-02 | Danh mục (taxonomy + catalog entries) | `VERIFIED` | `e2e/taxonomy.spec.ts` | Rebuilt 2026-08-24 against the redesigned reference layout: per-catalog URLs, group panel, server-side paging, drag/keyboard reordering. 5 specs — group + priced service persisted across reload, `/taxonomy/medicine` reached by link with `group=medication_type` on the wire, price column absent on unpriced catalogs, `skipCount` on the entry query, unmodelled catalogs explained |
| F-03 | Chấm công (lịch làm việc) | `VERIFIED` | `e2e/timekeeping.spec.ts` | Work day opened, shift clocked in and out through the UI |
| F-04 | Thu chi (phiếu thu/chi + duyệt) | `VERIFIED` | `e2e/finance.spec.ts` | Pending expense excluded from Tổng chi; approval adds exactly its amount |
| F-05 | Luân chuyển dòng tiền | `VERIFIED` | `e2e/finance.spec.ts` | Deposit moves Tổng Tiền Mặt by the deposited amount |
| F-06 | Hồ sơ bệnh nhân (đăng ký + danh sách) | `DIRTY` | `e2e/patient.spec.ts` | Registered through the UI, persisted, reopened from the list. **DIRTY 2026-08-27**: patients gained `TagIds` (uuid[] + migration), a "Phân loại Tag" multi-select in the editor and a wired tag filter on the list — builds/tests green but no runtime retest yet (skipped per user) |
| F-07 | Sơ đồ răng theo mặt | `VERIFIED` | `e2e/patient.spec.ts` | Whole-tooth, whole-jaw and clear all behave |
| F-08 | Voucher khuyến mãi | `VERIFIED` | `e2e/voucher.spec.ts` | Draft → active → paused; percentage above 100 refused; batch "Tạo một lượt": shared config + per-card override persisted (0/30 vs 0/70), card #1 mirrors single-tab code, missing name refused (7 tests, 2026-08-26) |
| F-09 | Chẩn đoán & Tư vấn | `VERIFIED` | `e2e/patient.spec.ts`, `e2e/treatment-stage.spec.ts`, `e2e/treatment-plan.spec.ts` | Rebuilt 2026-08-28 to the reference layout after a read-only survey — see `docs/clone/pages/patient-detail.md`. Image panel with the reference's three commands (`Thêm ảnh` file chooser, `Danh sách ảnh` → "Chọn ảnh hiển thị", `Danh mục` → "Dữ liệu tư vấn" read from `catalog-entries?group=consulting_data`), paired-fact diagnosis columns with `Tạo Dịch Vụ` / calendar / delete per row, the thirteen-column consulting sheet behind `Cấu hình cột`, and the TỔNG KẾ HOẠCH block with its %/VNĐ toggle, `Thêm kế hoạch điều trị`, `Tạo báo giá` and print. Both cards are the app's own `.bd-cat-card`. Two backend defects fixed: `PatientDiagnosisDto` and `PatientAdviseDto` declared name fields that were **never filled**, so six of the reference's columns read an em dash; `SecondStaffName` and `DiagnosisName` added and all of them now resolved one read per kind |
| F-10 | Lịch hẹn | `VERIFIED` | `e2e/appointment.spec.ts`, `e2e/patient-appointment.spec.ts` | Booking stored and found by a **server-side** search over every appointment; double-booking refused; day and week grids query their own range. The booking dialog is `main`'s (`AppointmentEditorModal` + the three form columns + colour picker + `AppointmentMiniCalendar`), which landed the reference layout, `Color` as a nullable `varchar(20)` and `Notes` on both create and update. This branch adds `initialPatientId` / `initialReason` / `lockPatient` so the patient screen can open it with the patient fixed, and fixes the zod/react-hook-form resolver typing that stopped the file compiling. 3 real full-stack tests on the production build: the tab fills the screen, the dialog's colour and note survive a reload, and an edit updates rather than duplicates. **DIRTY 2026-09-05**: "Lịch sử thay đổi" rebuilt on a real per-change log (`AppointmentChangeLog` + `AppointmentChangeRecorder` on every write of `AppointmentAppService`, migration `20260905000000_AddAppointmentChangeLog`, backfill seeder, `GET appointment-change-log` + `/stats`). Domain 11 / EF 5 / Application 18 tests green; `e2e/appointment-history.spec.ts` written but **not run** (user tests by hand). Needs an API restart for the migration and backfill. **VERIFIED again 2026-09-05** (Level 2): explicit `AppointmentChangeLogController` added, migration applied through DbMigrator, stats adapter fixed to read enum-name keys, null/"" no longer logged as a change; `e2e/appointment-history.spec.ts` 4/4 green on the real stack (booking → Tạo mới row with Web/IP/browser, edit → Cập nhật row + server-side action filter, timeline, empty week with the grey bar and empty card at full height); Domain 13 / Application 7 / Host conventions 14 green. See `features/patient.md` |
| F-11 | Tiếp nhận | `VERIFIED` | `e2e/reception.spec.ts` | Visit stored through the real API; counters served by `/visits/stats` |
| F-12 | CSKH | `VERIFIED` | `e2e/cskh.spec.ts` | Rebuilt 2026-08-26 for **staging.nfcdental.com** parity (new reference standard). 8 real full-stack tests on the production build: URL params (`tab`/`page`/`care_dateMode`/`care_date` with mode anchors), counters from `/stats` + status filtering, per-tab toolbar matrix, Tạo mới + inline note persisted across reload, care-result dialog (Thành công) persisted, Excel download `cskh-dac-biet-*.xlsx`, Phân nhóm CSKH (server search + base-care dialog), branch isolation via claim-scoped list **and a cross-branch create returning 404** (post-security-review guard in `CreateAsync`). See `features/customer-care.md`. **DIRTY 2026-08-27**: grouping `tagId` filter now real (patient `TagIds`) and `taxonomyId` extended to treatment-plan service-group membership — builds/tests green but no runtime retest yet (skipped per user) |
| F-30 | Bệnh án (hồ sơ bệnh án của bệnh nhân) | `VERIFIED` | `e2e/patient-medical-record.spec.ts` | Added 2026-08-31 as the record's second view, behind the header's Chi tiết hồ sơ / Bệnh án switch — the view rides in the URL as `?view=medical-record`. New aggregate `PatientMedicalRecord` + migration `20260831060000_AddPatientMedicalRecord`, an app service, an explicit controller at `api/v1/app/patient-medical-records`, and a new ability subject `patientMedicalRecord`. The index lists the reference's nine forms in its order, wording and colours; the canvas reuses **`MedicalRecordSheet`** — the A4 form Danh mục already draws for "Bệnh án mẫu" — rather than growing a second implementation. 2 real full-stack tests on the production build: the index and bottom bar carry what the reference carries, and a sheet is added, filled, reloaded out of PostgreSQL and removed. **Only form 2 draws** (the one Danh mục builds); the other eight are listed and say so — see `docs/clone/unknowns.md` |
| F-13 | Labo | `VERIFIED` | `e2e/labo.spec.ts` | Rebuilt 2026-08-27 as the reference's six sub-routes (`/labo/:section`) after a full read-only survey — see `docs/clone/pages/labo.md` and the Labo section of `docs/clone/api.md`. Khớp cắn / Đường hoàn tất / Kiểu nhịp file under the shared taxonomy the reference uses; Dịch vụ - vật liệu is a group panel beside its material table, with the material hanging off a group rather than a supplier; Nhà cung cấp gained the logo well, contact person, tax code and address the reference collects. Every table is the shared catalog card, `DataTable` and footer pager the Danh mục screens use, and **every search on every tab is a server round-trip**. Mẫu Labo gained the period picker, the patient and doctor filters and Xuất Excel, and lost its create button (the reference raises an order from the patient screen). Five real defects fixed on the way: `labo-suppliers`/`labo-materials` had no HTTP route and answered 404; `SampleFilter` was declared but never applied; `LaboOrder` was missing seven mapped columns; five labo entities carried no `ClinicBranchId`, so a row created in one branch was visible from every other; and every dialog in the app clipped the focus ring under its last field. 12 real full-stack tests on the production build. **Still to build**: the second status dimension (`statusClinic`), the returned-files column, the order detail modal and its print sheet — see `docs/clone/pages/labo.md` §9 |
| F-14 | Vật tư | `VERIFIED` | `e2e/materials.spec.ts` | Rebuilt 2026-08-26 as the reference's three sections (`/materials/clinic`, `/allocation`, `/department`). Materials file under `supplies` taxonomy groups through the panel Danh mục shares; a material and its group survive a reload; a department is created and survives a reload; the allocation list is real. Two app services that had no HTTP route (`departments`, `material-allocations`) were returning 404 to the browser and now have controllers. `InventoryItemDto.Status` was mapping to the enum's unnamed zero on every row and `TaxonomyName` was never filled — both are now stamped from the entity. 4 tests, run against the production build. See `docs/clone/pages/materials.md` for what the empty reference branch left UNKNOWN |
| F-15 | Quản trị vận hành | `VERIFIED` | `e2e/operations.spec.ts` | Article draft → published; task lifecycle; department travels with the query |
| F-16 | Công cụ (call/message/Zalo/hóa đơn) | `BLOCKED` | — | `UNKNOWN_REFERENCE_BEHAVIOR` — no data on the reference to observe |
| F-29 | Danh mục — màn hình phẳng (Đơn thuốc mẫu, Thẻ hồ sơ, Phương thức thanh toán) | `VERIFIED` | `e2e/taxonomy-flat.spec.ts` | Added 2026-08-24. Đơn thuốc mẫu drops the group panel and the group column while Bệnh án mẫu keeps both; a coloured thẻ hồ sơ is stored and survives a reload; MoMo and bank accounts stay on their own tabs across a reload; an account with no holder is refused. New tables `bd_patient_tags` (branch-scoped) and `bd_payment_accounts` |
| F-31 | Danh mục — ảnh QR phương thức thanh toán | `VERIFIED` | `e2e/payment-qr.spec.ts` | Added 2026-08-24. "Tải ảnh QR" uploads through a real multipart POST to MinIO, the saved QR is served back by the API and survives a reload, removal clears both row and blob, and a non-image file is refused before it leaves the browser. New columns on `bd_payment_accounts` (`QrImage*`); the reference's own limits could not be observed — see `docs/clone/unknowns.md` Re-verified 2026-08-25 after the rebase onto `main` (Ant Design 6, Tailwind removed) — the catalog UI was rebuilt on antd + a named stylesheet, and the suite was run against the **production build** (`vite preview`), not the dev server: see R-69…R-76 in `03-regression-log.md`. **Chốt 2026-08-25 — màn hình Danh mục coi như hoàn thiện; xem mục 17 của CLAUDE.md trước khi sửa.** |
| F-32 | Danh mục — nhóm phân loại (dialog, tìm kiếm, kéo-thả) | `VERIFIED` | `e2e/taxonomy-groups.spec.ts`, `e2e/taxonomy.spec.ts` | Added 2026-08-24. `Mức độ ưu tiên` is stored and decides the order, and comes back into the dialog on edit; the search sends `filter=` to the API and empties the panel on a term the server cannot match; search is trimmed, case-insensitive and term-wise across every column shown (`TRÁM RĂNG SỨ`, `   trám răng sứ   ` and `sứ trám` all find the same row; whitespace alone is not a search); a real pointer drag reorders the panel as it passes rows and saves the whole order in **one** `POST /taxonomies/reorder`; the entry table does the same through `POST /catalog-entries/reorder`, with the lifted row following the pointer sideways as well as down Re-verified 2026-08-25 after the rebase onto `main` (Ant Design 6, Tailwind removed) — the catalog UI was rebuilt on antd + a named stylesheet, and the suite was run against the **production build** (`vite preview`), not the dev server: see R-69…R-76 in `03-regression-log.md`. **Chốt 2026-08-25 — màn hình Danh mục coi như hoàn thiện; xem mục 17 của CLAUDE.md trước khi sửa.** |
| F-33 | Danh mục — parity dialog (P1+P2) | `VERIFIED` | `e2e/taxonomy-groups.spec.ts` | Added 2026-08-25. Khung dialog dùng chung theo bản gốc (tiêu đề + kẻ, floating label, một nút `Lưu`, không `Huỷ`); menu hàng nhóm còn `Chỉnh sửa`/`Xoá` với grip nhận phím `↑`/`↓`; ba tab đơn giản dùng đúng bộ field của bản gốc và `Đã xoá` xoá bản ghi khi lưu; tab Nghề nghiệp không có nút `Xuất`. P3–P7 còn lại — xem `save/taxonomy-parity-plan.md` Re-verified 2026-08-25 after the rebase onto `main` (Ant Design 6, Tailwind removed) — the catalog UI was rebuilt on antd + a named stylesheet, and the suite was run against the **production build** (`vite preview`), not the dev server: see R-69…R-76 in `03-regression-log.md`. **Chốt 2026-08-25 — màn hình Danh mục coi như hoàn thiện; xem mục 17 của CLAUDE.md trước khi sửa.** |
| F-34 | Danh mục — dialog theo từng danh mục (P3–P7) | `VERIFIED` | `e2e/taxonomy-dialogs.spec.ts` | Added 2026-08-25. Dịch vụ giữ đủ cấu hình giá/thuế, công đoạn và bảo hành, và hai ô tính ra (`Giá sau giảm` 900, `Thực thu` 990 từ giá 1.000 giảm 10% cộng VAT 10%) trả về từ server; thuốc giữ hoạt chất và cả hai giá; chẩn đoán giữ nội dung rich-text và ghi chú; đơn thuốc mẫu lưu dòng thuốc và tự tính `Số lượng`; bệnh án mẫu lưu tờ A4 đã điền. Tờ A4 dựng lại 2026-08-25 theo đúng bản gốc — 3 trang, 17 ô nhập, test khẳng định cả tiêu đề in sẵn lẫn ô nhập trên cả ba trang. 4 bảng mới + 3 cột trên `bd_catalog_entries` Re-verified 2026-08-25 after the rebase onto `main` (Ant Design 6, Tailwind removed) — the catalog UI was rebuilt on antd + a named stylesheet, and the suite was run against the **production build** (`vite preview`), not the dev server: see R-69…R-76 in `03-regression-log.md`. **Chốt 2026-08-25 — màn hình Danh mục coi như hoàn thiện; xem mục 17 của CLAUDE.md trước khi sửa.** |
| F-35 | Vận hành — phân loại và bài viết theo khối | `VERIFIED` | `e2e/operations.spec.ts` | Added 2026-08-25. Rà soát lại toàn bộ theo bản gốc cùng ngày (R-93→R-98): tám khối là tám route, **mỗi khối một tham số sub-tab riêng** và chúng cộng dồn trong URL nên rời đi rồi quay lại vẫn đúng tab; sub-tab lấy đúng từng khối (Khối bảo vệ 3, Khối tài chính 6 tab riêng); Khối điều trị/tài chính có thêm hàng tab giữa Tổng quan/Truy cập. **Chỉ Trang chủ/Quy trình/Công việc là màn bài viết** — 6 sub-tab còn lại là báo cáo, render `OperationReportPanel` chứ không giả vờ là màn bài viết; cột của chúng ghi ở `docs/clone/pages/operations.md`. Panel phân loại dựng riêng theo bản gốc (không tiêu đề/số đếm/mô tả/tìm kiếm/kéo-thả, hành động hiện khi hover). Hai dialog theo đúng chữ và số đo đọc từ DOM. Ảnh trong bài viết lưu ra blob, nội dung chỉ giữ link. Báo cáo và nút xoá của bản gốc còn `UNKNOWN` — xem `docs/clone/unknowns.md` |
| F-36 | Vận hành — bảy màn báo cáo | `VERIFIED` | `e2e/operations-reports.spec.ts`, `OperationsReportWindowTests` | Added 2026-08-26. Sáu endpoint chỉ-đọc (`/operations/reports/...`), lọc theo chi nhánh và theo kỳ Ngày/Tuần/Tháng/Năm với ngày mốc được server làm tròn về kỳ. Báo cáo gộp 5 nguồn thành một nhật ký và lọc theo hành động; Chẩn đoán chưa điều trị đọc `HasTreatmentService = false`; Khách hàng phát sinh gộp theo nhân sự tư vấn, tách khách mới/cũ theo việc phòng khám đã tư vấn người đó trước kỳ hay chưa; Hóa đơn; Hoàn thành theo dịch vụ với 5 thẻ số (gồm % so với kỳ trước); Truy cập dùng chung cho Khối điều trị và Khối tài chính, 3 thẻ số kiêm luôn bộ lọc. Đơn thuốc giữ đúng câu của bản gốc vì bản gốc cũng chưa dựng. Dữ liệu demo: `BlueDentalReportsDemoSeeder` rải 120 ca trên 75 ngày — xem R-99→R-103 về hai lỗi seeder phải sửa trước. Công thức các thẻ số của bản gốc còn `UNKNOWN` — xem `docs/clone/pages/operations.md` |
| F-37 | Chăm sóc KH — tab trong hồ sơ bệnh nhân | `VERIFIED` | `e2e/patient-care.spec.ts` (2), `CustomerCareAppServiceContractTests` | Rebuilt 2026-09-05 to the staging reference after a read-only survey and the owner's answers (see `docs/clone/pages/patient-detail.md`, Tab 8, and `features/customer-care.md`). Eight single-select counters (Đã chăm sóc · Tốt · Khá · Bình thường · Khiếu nại | Đặc biệt · Định kỳ · Cơ bản) each mapping to one server-side param (`status` / `outcome` / `type`), `+ CSKH đặc biệt`, the nine reference columns with a Chi tiết link → "Chi tiết phiếu", Chỉnh sửa → "Cập nhật chăm sóc khách hàng" and Xoá → "Xóa lượt chăm sóc". Backend: `Outcome` filter on the list, per-type stats (`special` / `periodic` / `base`), `outcome` accepted on update, `DELETE care-records/{id}` (soft delete, Manage permission, branch guard). Real-stack spec on the production build: create with Tốt → row + three counters move → Khiếu nại chip hides it, Tốt chip shows it, second click clears → detail dialog → edit to Khiếu nại (PUT) survives a reload → delete (DELETE) survives a reload and the counter returns to baseline. Deviations: house chrome and the shared 440px confirm dialog |
| F-17 | Báo cáo doanh số | `DIRTY` | `e2e/report.spec.ts` | 2026-09-04: `/report` dựng lại theo bản gốc, FE-only trên mock data (`reportMockQueries.ts`); mọi nút ghi chỉ hiện toast demo. `e2e/report.spec.ts` viết lại cùng ngày (4 test xanh trên `vite preview` :8080, đăng nhập thật, khẳng định không có POST/PUT/PATCH/DELETE nào tới `/api/`) — nhưng chỉ chứng minh UI + không ghi, **không** phải bằng chứng runtime vì dữ liệu là mock. `/clinic-reports` không còn được UI gọi. Retest Level 2 khi nối BE. 2026-09-04 (chiều): Xuất Excel sub-tab Thanh toán dựng lại theo file gốc — 15 cột đúng thứ tự (tách mã/tên khách, thêm Người tạo, Mã phiếu điều trị, Chi nhánh, Thông tin thanh toán, Ghi chú; "Tổng tạm ứng còn lại" đứng trước "Thực thu"), số tiền ghi ô số thô; `report.spec.ts` mở file tải về và so header + kiểu ô. Xem `docs/clone/pages/report.md` |
| F-18 | Kết quả kinh doanh | `DIRTY` | `e2e/report.spec.ts` | 2026-09-04: cùng đợt dựng lại với F-17 — `BusinessResultTab` đọc `useMockBusinessResult`; 6 dòng theo đúng thứ tự bản gốc, chưa có BE |
| F-19 | Công đoạn điều trị | `VERIFIED` | `e2e/treatment-stage.spec.ts` | Chưa làm → Đang làm → Hoàn thành, persisted. **Model is BlueDental's assumption** — see below |
| F-20 | Phân tách chi nhánh | `VERIFIED` | `e2e/branch-isolation.spec.ts` | Rewritten 2026-08-24 for the stricter contract: naming a branch the account may not work in answers **403** rather than being silently narrowed; `manager` (no assignment) reaches both branches; an unqualified list still narrows to the caller's own |
| F-30 | Chuyển chi nhánh trên header | `VERIFIED` | `e2e/branch-switcher.spec.ts` | Added 2026-08-24, extended the same day. The switcher lists only branches the account may work in; a branch-scoped account neither sees the other branch's catalog nor writes into it; a clinic-wide account (`manager`) switches between both branches and each shows its own seeded catalog, persisted across a reload. The popover now closes on select |
| F-21 | Phiếu điều trị + dòng dịch vụ | `VERIFIED` | `e2e/treatment-plan.spec.ts` | Accepted advise becomes a priced service line on DT01; a line cannot be planned twice |
| F-22 | Thanh toán bệnh nhân | `VERIFIED` | `e2e/treatment-plan.spec.ts` | Only finished work becomes Phải thu; refund capped at what was collected; prepaid held, not counted as paid |
| F-23 | Đơn thuốc | `VERIFIED` | `e2e/prescription.spec.ts` (5), `PrescriptionTests`, `PrescriptionAppServiceContractTests`, `PrescriptionMappingTests` | **Rebuilt 2026-09-05** to the staging reference and the owner's answers (see `docs/clone/pages/patient-detail.md`, Tab 7, and `features/prescription.md`). Tab = right-aligned "Tạo đơn thuốc" over Mã đơn thuốc · Bác sĩ · Chẩn đoán · Tái khám · Ngày tạo · Thao tác (Sửa/Xóa), "Hiển thị n trên m" pager even when empty; the dialog rides in the URL as `create=true`, shows the patient block, template picker, doctor (required), diagnosis, lời dặn, "Lưu đơn thuốc mẫu" → "Tên đơn thuốc mẫu", Điều trị, Tái khám and the medicine lines (Số lượng = Ngày uống × Mỗi lần × Số ngày, derived). Lines rebuilt as `bd_prescription_items` with timesPerDay/amountPerTime/days/usage (migration `20260905120000_RebuildPrescriptionLines`); saving with the box ticked files a `prescription_template` catalog entry under the typed name, and picking it back prefills lines + lời dặn. Real-stack spec: create with lines + template → reload → template offered and prefilled; Sửa persists (PUT); branch2 gets **403** on the slip; Xóa confirms then DELETEs and the row is gone after reload. The line editor was lifted into `src/components/prescription-lines/` and the Đơn thuốc mẫu dialog now uses it — the taxonomy/payment-qr/branch suite re-run on the production build is 42 specs: 39 green, 3 red that fail identically on a clean HEAD `20c4815` build (see the 2026-09-05 regression-log entry). Earlier note (2026-08-28): the template picker used to call a route that did not exist |
| F-24 | Hình ảnh bệnh nhân | `VERIFIED` | `e2e/patient-image.spec.ts` | Real multipart upload to MinIO, bytes fetched back through the API. **2026-09-05**: tab rebuilt to the reference — `type` (Trước/Sau điều trị) filter, day timeline, `ordering` with `PUT /reorder`, in-house viewer, confirm-then-delete. The spec uploads two pictures, filters, views, drags one past the other and reloads, deletes both, and checks `branch2` gets **403** on branch one. `current-user` now returns the granted permissions, which the tab's upload / sort / delete buttons depend on. Same day: viewer zoom by wheel / double-click, drag-to-pan, palette with a custom colour, strokes riding inside the picture frame; all exercised in the viewer test |
| F-25 | Nhân viên | `VERIFIED` | `e2e/staff.spec.ts` | Account created, edited and deleted; weak password refused by Identity |
| F-28 | Tìm kiếm nhanh (Ctrl K) | `VERIFIED` | manual browser run, 2026-08-24 | The palette used to render a search box with no `value`, no `onChange` and no results. It now queries `/patients?filter=` and opens the record; typing TRAN returned 8 real patients and the first one opened `/patient/:id` |
| F-27 | Thanh toán & hoá đơn (màn hoá đơn phòng khám) | `VERIFIED` | manual browser run, 2026-08-24 | Payment recorded through the real modal on `HD-202608-0012`; PostgreSQL shows `paid_amount=1000000`, `Status=3 (PartiallyPaid)`; survives a reload. Excel export returns a real `.xlsx` |
| F-26 | Song ngữ Việt/Anh (i18n) | `VERIFIED` | manual browser sweep, 2026-08-23 | Switch is instant, no reload; 985 keys, 0 untranslated; survives reload via `localStorage`; Zod messages and `Accept-Language` follow the switch |
| F-38 | Hồ sơ bệnh nhân — thẻ hồ sơ, lý do đến khám, hàng tiền, bảng điều trị | `VERIFIED` | `e2e/patient.spec.ts` (3 mới), `PatientTests`, `PatientMappingTests` | Ba lệch chủ dự án chỉ ra, sửa 2026-09-06 sau khảo sát chỉ đọc trên staging (`docs/clone/pages/patient-detail.md`, Tab 1). (1) **Tag**: dấu ✓ trong picker vẽ trắng-trên-trắng vì `CheckOutlined` ra `<span>` trần, trúng rule chip của hàng — chip tách thành `.pd-tag-chip`; nhãn của hồ sơ nay hiện cạnh tên theo thứ tự danh mục, cùng hàng `flex-wrap` với tên + bút chì, chọn được nhiều tag, lưu ngay. (2) **Lý do đến khám** đổi từ một chuỗi sang danh sách có ngày: entity con `PatientExaminationReason` + bảng `bd_patient_examination_reasons` (migration `20260906000000_AddPatientExaminationReasons` mang giá trị cũ sang làm dòng gốc), `POST patients/{id}/examination-reasons` cho nút **+** (thêm dòng), dialog "Chỉnh sửa hồ sơ" vẫn sửa dòng gốc tại chỗ — đúng như bản gốc. (3) **Tạm ứng**: hàng tiền lên 7 ô, đọc `payment.prepaid` đã có sẵn. Real-stack trên bản build production: thêm hai lý do → in ngày `dd/MM/yyyy` cột 88px + nội dung đỏ, mới-trước, sống qua reload; bật một tag → tích trong picker + chip cạnh tên, sống qua reload; 7 ô tiền trên một hàng, `Tạm ứng` khớp con số server trả. `e2e/patient*.spec.ts` 41/41 xanh, `branch-isolation` 5/5. **Đợt 2 cùng ngày** (R-183…R-187): lý do đến khám hạ về 13.5px/20 cho khớp nấc chữ của cột; filter "Phân loại theo Tag" bỏ chip màu bịa, trả về dòng chữ thuần như "Phân loại dịch vụ" (bỏ luôn khỏi `SearchSelect`, kéo theo Phân nhóm CSKH); thẻ Lịch hẹn gần nhất có combobox đổi bác sĩ ngay tại chỗ — PUT gửi nguyên lịch hẹn vì `UpdateAsync` dựng lại slot + details từ request; bảng điều trị dựng lại đủ **10** cột theo số đo bản gốc (th sentence-case 14px, ô kẻ 12/16, chip mã DT + trạng thái, chip răng, "Phụ tá:", nút + xanh cho công đoạn, cột "Chăm sóc sau điều trị") và Thao tác mở **"Tạo phiếu thanh toán"** 1024px — mỗi dòng dịch vụ một `POST patient-payments` mang `treatmentServiceId`, nên "Còn nợ" của đúng dòng đó mới đổi. BE thêm `stageNotes` / `paidAmount` / `outstandingAmount` / `afterCareStatus` lên `TreatmentServiceDto`. `e2e/patient*.spec.ts` **50/50**, Domain 250 / Application 516 / EF 51 xanh, `treatment-plan` 4/4, `treatment-stage` 2/2. **Đợt 3 cùng ngày** (R-188…R-192): nút cột Công đoạn mở **"Chi tiết phiếu"** dựng theo bản gốc (2 tab THÊM/TIẾP TỤC CÔNG ĐOẠN có badge đếm, Thanh toán + In lịch sử điều trị, 4 tiêu đề cột, form Ngày tạo/Dịch vụ disabled + Bác sĩ/Phụ tá/Bác sĩ hỗ trợ + chip răng + Nội dung điều trị, bảng LỊCH SỬ ĐIỀU TRỊ kẻ ô với Hoàn thành / Tải ảnh / Tạo Labo) — Lưu gọi `POST treatment-stages` thật; modal thanh toán soi lại từng field (tiêu đề section viết hoa + icon, hàng fact 150px canh trái, pill Còn nợ, lỗi hiện ngay, radio chia tiền, nhãn nổi + 0/500, phương thức là pill bo tròn có badge icon) và bỏ ô "Số tiền của phiếu" tự bịa; thêm **Ví momo** — `PaymentMethodKind.EWallet = 5` cùng `ByEWallet`/`RefundByEWallet` trên báo cáo phòng khám để tiền ví không rơi khỏi tổng; `Còn lại` tính sống theo số đang nhập; Chia Tiền Thủ Công ra một ô cho mỗi dịch vụ đã tích, mồi sẵn Còn nợ. `UploadPatientImageInput` thêm `treatmentStageId`; `useStageMutation` invalidate thêm `patient-treatments`. e2e **67/67** (`patient*`, `treatment-plan`, `treatment-stage`, `report`, `branch-isolation`). **Đợt 4 cùng ngày** (R-193…R-196), đọc hợp đồng API từ **bundle** của bản gốc (static asset — rule 00 cho phép) nên **không ghi một byte nào** lên staging; kiểm trước thì thấy tài khoản khảo sát không có ability `payment` (403) và `treatmentStage` không có `delete`, tức là ghi vào cũng không hoàn tác được. Thu được: `/v1/payment-v2` (create · PATCH · void · finalize · export) với schema Joi đầy đủ, và `/v1/patient-stages` (create · continue · updateStatus · revertStatus · stage-service-items). Áp dụng: **`paymentAccountId`** bắt buộc khi chọn Ngân hàng / Ví momo — bảng chọn tài khoản đọc từ danh mục `PaymentAccount`, cột mới `PatientPayment.PaymentAccountId` (migration `20260906120000_...`) và guard ở aggregate; ô tiền Tự động **mồi sẵn** theo công thức của bản gốc (Dư nợ bị chặn ở số đang giữ); câu báo vượt quá dùng đúng chữ bản gốc; form công đoạn bắt buộc bác sĩ + răng + nội dung (max 1000). **Đợt 5 cùng ngày** (R-197…R-200) đóng nốt lệch cuối: một phiếu thu mang **nhiều** dịch vụ, đúng như bản gốc. `PatientPayment` có bảng con `PatientPaymentLine` (`treatmentServiceId`, `amount`) và cột `SplitMode`; bỏ `PatientPayment.TreatmentServiceId` (migration `20260906140000_AddPatientPaymentLines` backfill mỗi phiếu cũ thành một dòng rồi mới drop cột). Chia tiền chuyển hẳn về server: **Tự động** chỉ gửi tổng, server rải từ dòng cũ nhất và chặn ở đúng Còn nợ từng dòng; **Thủ công** gửi `items[]`. Aggregate từ chối dòng bằng 0 và phiếu có tổng các dòng lệch tổng phiếu (`BlueDental:Billing:0091`); rollup `paidAmount` đọc theo dòng phiếu nên hoàn tiền vẫn trừ đúng dòng. Vẫn **không ghi gì lên bản gốc** — xác minh bằng dữ liệu local. Real-stack: dựng một phiếu hai dòng bằng chính API của app, thu 3.000.000 trên 4.900.000 → **một** POST mang cả hai `treatmentServiceIds`, `splitMode: 1`, không `items`; đọc lại đúng một phiếu, dòng 2.400.000 + 600.000, `paid 2.400.000 / due 0` và `paid 600.000 / due 1.900.000`, lịch sử hiện **một** dòng. Ba spec đỏ sau đổi schema là do dữ liệu demo nay có phiếu nhiều dòng nên "dòng đầu bảng" ≠ "dòng vừa bấm" — đã sửa cho độc lập thứ tự (bấm theo `tr[data-row-key]` của dòng còn nợ, cộng Còn nợ theo các dòng đang tích, đọc phiếu theo id dịch vụ chứ không theo thứ tự dòng). e2e **49/49** (`patient`, `patient-images`, `treatment-plan`, `treatment-stage`, `report`, `branch-isolation`), Domain **262** / Application **516** / EF **51** xanh. **Đợt 6 cùng ngày** (R-201…R-214) rà lại "Chi tiết phiếu" theo từng nút, từng modal ẩn, vẫn **không ghi gì lên bản gốc** (mở dialog rồi đóng, đọc markup và bốn GET dialog tự gọi): form công đoạn kín cột (đo 366px = đúng bề rộng cột ở 1600) và xuống 2 cột trước khi xuống 1, răng thành chip xanh đặc, `Hình ảnh:`/`(Trống)` tách hai dòng, bỏ dấu sao ở nhãn ghi chú, `Tải Ảnh` trong form bật sẵn (ảnh chọn trước được đính sau khi lưu); LỊCH SỬ ĐIỀU TRỊ gộp theo ngày với ô Ngày trải hết ngày đó, in `d/M/yyyy`, ảnh công đoạn thành ô 68px mở viewer, bút chì sửa ghi chú tại chỗ qua `PUT treatment-stages/{id}`, và **Phụ tá / Bác sĩ hỗ trợ được lưu thật** (`TreatmentStage.SubStaffId` mới — migration `20260906160000_AddStageSubStaff` — cùng `subStaffName`/`secondStaffName`); ba hành vi ẩn dựng đúng bản gốc: **In lịch sử điều trị** mở modal in kèm tờ A4 ẩn có hai ô ký, **Thanh toán** rời dialog sang Kế hoạch điều trị, **Tạo Labo** mở dialog **"Đặt mới"** mồi sẵn từ công đoạn (`LaboOrder` thêm `ToothShade`/`Quantity`/`TreatmentServiceId`/`TreatmentStageId` — migration `20260906170000_AddLaboOrderTreatmentLink` — và `GET labo-orders/next-code` sinh `LABO-yyyyMMddN`); dòng dịch vụ đã hoàn thành đổi sang chip xám không bấm được, khớp bộ lọc `status=created,inProgress,guarantee` của bản gốc. e2e **69/69** (thêm `labo`), Domain **264** / Application **516** / EF **51** xanh. **Đợt 7 cùng ngày** (R-215…R-220), vẫn chỉ đọc bản gốc: bảng điều trị đổi sang **một dòng cho mỗi công đoạn** đúng như `/v1/patient-timeline` (`type: "stage"`), ô Ngày trải hết ngày bằng `rowSpan` và tính lại span sau khi lọc, ô Công đoạn rút về hai trạng thái (+ hoặc chip xám) vì con số của bản gốc là checklist danh mục chứ không phải số công đoạn; cờ `disabled` của bản gốc được dựng lại — chỉ công đoạn **mới nhất** của một dịch vụ còn sống, cái cũ `aria-disabled` + mờ + `pointer-events: none`, ô Hoàn thành khoá và mất nút Tạo Labo; dialog Labo **"Đặt mới"** dựng lại bằng chính field component của source (`SearchSelect` trong `FloatingLabel`, `FloatingLabel` nay có `required` in dấu sao đỏ trong nhãn — 11 field bắt buộc, khớp bản gốc) theo đúng thứ tự khối, hai dải chip có pill viền đứt khi rỗng, hàng `Răng:*` có "Chọn tất cả", và ô vuông 80px Tải ảnh. e2e **72/72**. **Đợt 8 cùng ngày** (R-221…R-228), vẫn chỉ đọc bản gốc: ô Công đoạn suy từ **chính công đoạn** của dòng chứ không từ dòng dịch vụ, và có ba trạng thái — + xanh (chưa xong), chip hổ phách **Bảo hành** (đã xong, dịch vụ có kỳ bảo hành), chip xám "Không bảo hành"; thêm công đoạn mới **không** đóng các công đoạn cũ (cờ `disabled` của bản gốc chỉ làm mờ dòng trong dialog, ngoài bảng mọi công đoạn chưa xong vẫn bấm được); dialog **"Tạo bảo hành"** dựng theo bản gốc và ghi một công đoạn `isGuarantee` (cột mới `TreatmentStage.IsGuarantee`, migration `20260906180000_AddStageGuarantee`; `TreatmentServiceDto.WarrantyDays` đọc từ `CatalogServiceConfig`, seed demo có cả dịch vụ có và không có bảo hành); ba bước **Tiếp nhận** nay bấm được lần lượt qua `check-in`/`start`/`complete` và in giờ thật; **"Tạo tái khám"** liệt kê công đoạn đã hoàn thành. Kèm một lỗi seed do đợt R-193 để lại: phiếu ngân hàng demo không kèm tài khoản nên aggregate từ chối — chỉ lộ khi chạy migrator ở Development. e2e **78/78** (thêm `appointment`), Domain **264** / Application **516** / EF **51** xanh |

## F-19 note — assumed, not observed

The reference never exposed a treatment-stage payload that could be read without
mutating production. What was observed is only: the ability subject
`treatmentStage` and its six verbs, the per-service "Thêm công đoạn" action,
`stageIds` / `patientStages[]` on CSKH records, and `stageNote` in the treatment
summary. Everything else — sequence numbers, tooth selection, timestamps, and the
image rule — is BlueDental's own design and is documented as such in
`TreatmentStage`.

## Backend regression nets (not acceptance)

| Suite | Count | Last run |
|-------|-------|----------|
| `BlueDental.Domain.Tests` | 196 | 2026-08-26 — pass |
| `BlueDental.Application.Tests` | 474 | 2026-08-26 — pass |
| `BlueDental.EntityFrameworkCore.Tests` | 39 | 2026-08-24 — pass |
| `BlueDental.HttpApi.Host.Tests` | 15 | 2026-08-24 — pass |
| `BlueDental.FE` Vitest | 3 | 2026-08-24 — pass |

## Still not covered

- **Công cụ** (F-16): gọi điện / SMS / Zalo / hoá đơn điện tử — the reference had
  no data to observe.
- **Xuất Excel / PDF**: wired on patients, labo, CSKH, reports, prescriptions,
  treatment plans and invoices. Screens without an export endpoint still have no
  button.
- **i18n export language**: a PDF or Excel export is still generated in
  Vietnamese regardless of the UI language.
- **i18n on the login screen**: the switcher lives in the app header, which only
  exists after sign-in, so the sign-in page is always Vietnamese.
- Two catalogs ("Thẻ hồ sơ", "Phương thức thanh toán") that the reference does not
  model as catalogs.
