# Feature Verification Registry

Statuses: `NOT_STARTED` · `IN_PROGRESS` · `READY_FOR_TEST` · `FAILED` ·
`VERIFIED` · `DIRTY` · `BLOCKED`.

Only `VERIFIED` means the feature passed **real runtime acceptance** — real
browser, real API, real PostgreSQL (see `00-test-policy.md`).

Last run: 2026-08-24, stack on `localhost:5173` (Vite) → `localhost:5019` (API)
→ PostgreSQL 15 and MinIO in Docker.

> ⚠️ **The suite is not green.** The last full run was **51 passed, 30 failed**.
> Sixteen of those failures are specs still written against Ant Design selectors
> (`tr.ant-table-row`), and `antd` is no longer a dependency; three more are forms
> whose `<label>` carries no `htmlFor`. They fail in isolation too, so they are rot
> rather than interference. Do not read a run through `| tail -N`: the exit code
> then comes from `tail` and the failure list scrolls away — that is how earlier
> "N passed" numbers in this file were produced. Every `VERIFIED` row backed by one
> of the failing specs needs re-checking. See `save/taxonomy-handoff.md` §6.

| ID | Feature | Status | Acceptance spec | Notes |
|----|---------|--------|-----------------|-------|
| F-01 | Đăng nhập | `VERIFIED` | `e2e/fixtures/auth.ts` (used by every spec) | Real login form, real cookie auth |
| F-02 | Danh mục (taxonomy + catalog entries) | `VERIFIED` | `e2e/taxonomy.spec.ts` | Rebuilt 2026-08-24 against the redesigned reference layout: per-catalog URLs, group panel, server-side paging, drag/keyboard reordering. 5 specs — group + priced service persisted across reload, `/taxonomy/medicine` reached by link with `group=medication_type` on the wire, price column absent on unpriced catalogs, `skipCount` on the entry query, unmodelled catalogs explained |
| F-03 | Chấm công (lịch làm việc) | `VERIFIED` | `e2e/timekeeping.spec.ts` | Work day opened, shift clocked in and out through the UI |
| F-04 | Thu chi (phiếu thu/chi + duyệt) | `VERIFIED` | `e2e/finance.spec.ts` | Pending expense excluded from Tổng chi; approval adds exactly its amount |
| F-05 | Luân chuyển dòng tiền | `VERIFIED` | `e2e/finance.spec.ts` | Deposit moves Tổng Tiền Mặt by the deposited amount |
| F-06 | Hồ sơ bệnh nhân (đăng ký + danh sách) | `DIRTY` | `e2e/patient.spec.ts` | Registered through the UI, persisted, reopened from the list. **DIRTY 2026-08-27**: patients gained `TagIds` (uuid[] + migration), a "Phân loại Tag" multi-select in the editor and a wired tag filter on the list — builds/tests green but no runtime retest yet (skipped per user) |
| F-07 | Sơ đồ răng theo mặt | `VERIFIED` | `e2e/patient.spec.ts` | Whole-tooth, whole-jaw and clear all behave |
| F-08 | Voucher khuyến mãi | `VERIFIED` | `e2e/voucher.spec.ts` | Draft → active → paused; percentage above 100 refused; batch "Tạo một lượt": shared config + per-card override persisted (0/30 vs 0/70), card #1 mirrors single-tab code, missing name refused (7 tests, 2026-08-26) |
| F-09 | Chẩn đoán & Tư vấn | `VERIFIED` | `e2e/patient.spec.ts`, `e2e/treatment-stage.spec.ts`, `e2e/treatment-plan.spec.ts` | Rebuilt 2026-08-28 to the reference layout after a read-only survey — see `docs/clone/pages/patient-detail.md`. Image panel with the reference's three commands (`Thêm ảnh` file chooser, `Danh sách ảnh` → "Chọn ảnh hiển thị", `Danh mục` → "Dữ liệu tư vấn" read from `catalog-entries?group=consulting_data`), paired-fact diagnosis columns with `Tạo Dịch Vụ` / calendar / delete per row, the thirteen-column consulting sheet behind `Cấu hình cột`. Both cards are the app's own `.bd-cat-card`. Two backend defects fixed: `PatientDiagnosisDto` and `PatientAdviseDto` declared name fields that were **never filled**; `SecondStaffName` and `DiagnosisName` added. **Rebuilt again 2026-09-07 (FE only, staging as the source)**: the diagnosis editor opens inline in the card (`PatientDiagnosisForm`, `DiagnosisModal` deleted) with the two-doctor row, the shared `ToothPickerTabs` / `DentitionRadio` / `ToothChart`, a 260px side column and 28px "Răng đã chọn" chips; tooth wording "18, 16 - Mặt ngoài, Mặt nhai" via `surfaceLabel`; "Thêm chẩn đoán" deliberately disabled; the TỔNG KẾ HOẠCH footer follows staging — no %/VNĐ toggle, a "Voucher áp dụng" picker fed by `GET vouchers/available?orderAmount=<ticked total>` (treatment-scope vouchers as green-when-picked cards, search, multi-pick shown as "Voucher (n)", client-side discount off "Tổng tiền"; persistence of the pick still UNKNOWN on the reference), `Chọn bác sĩ điều trị` 296×40, `Thêm kế hoạch điều trị`, `Tạo báo giá`, 36px print. Drag handle on the advise table not built (no reorder endpoint). Level 2 retest on the production build (:8081, real API/DB): `patient.spec.ts` consulting 4/4 incl. a new real save-and-reload test, then 6/6 on :8083 after R-263/R-264 (the plan total sums only the ticked rows; the voucher popover loads real vouchers and a pick comes off the total — 4 stage tests red from another session's new "image required" rule, see log), `treatment-stage.spec.ts` 2/2, `treatment-plan.spec.ts` 5/5, `treatment-plan-detail.spec.ts` 6/6 (R-252 → R-257). **2026-09-08** — four mismatches the project owner found, re-measured on staging and rebuilt: the photographs now fill the panel and the grey "Kéo ảnh vào" box only stands in for them (it used to sit above them); "Chọn ảnh hiển thị" drags for real, on the Hình ảnh tab's own card (`PatientImageCard` + the new shared `PatientImageSortableRow`) and saving through `PUT /patient-images/reorder`; `Danh mục` opens the full **Dữ liệu tư vấn** library modal (`ConsultingLibraryModal` — topics, content cards, zoom / X-ray invert / pen, Toàn màn hình) rather than a popover; and the calendar icon in the diagnosis row is the reference's **In chẩn đoán**, which opens the A4 sheet (`DiagnosisPrintDialog`) whose `Cập nhật` saves `{contentDiagnosis, note}` to the new `PUT /v1/app/patient-diagnoses/{id}/print-content` (column `ContentDiagnosis`, migration `20260908000000_AddDiagnosisPrintContent`). Five real-stack specs cover it, including a drag that survives a reload and a saved advice that survives a reload — see the 2026-09-08 regression-log entry. **2026-09-08 (chiều)** — năm việc chủ dự án nêu trên chi nhánh 2: voucher seed theo **từng** chi nhánh (`SeedVouchersAsync(branchId)`, list riêng cho chi nhánh 2 vì list cũ đặt ngưỡng gấp 4 lần giá trị); panel ảnh giữ `Spin` tới khi ảnh về thay vì nháy ô "Kéo ảnh vào"; bảng Phiếu tư vấn kéo thả thật qua **`PUT /v1/app/patient-advises/reorder`** (đánh số lại 1..N, dùng lại `useDragReorder`/`bd-grip` của Danh mục); cột Dịch vụ chỉ còn tên và cột Chẩn đoán gộp "răng - tên" kèm ghi chú phiếu; chẩn đoán dưới số răng đổi sang **#12A960**; bốn lệnh chân phiếu khoá tới khi tick một dòng, "Thêm kế hoạch điều trị" validate bác sĩ và báo lỗi **dưới ô**; "Tạo báo giá" thôi gọi `window.print()`; và hai tờ in con có lại **chân chữ ký** (`QuoteSignatures`). Retest mức **2**: `e2e/consulting-plan.spec.ts` 7/7 và `patient.spec.ts` 63/63 xanh trên bản build production (:8080, API :5019, DB thật), API thật kiểm voucher theo ngưỡng và reorder có cách ly chi nhánh (403). R-327 → R-335. `patient-image.spec.ts` còn 3 đỏ thuộc tab Hình ảnh, không thuộc đợt này — xem regression log. **2026-09-08 (tối)** — bốn chỗ nữa: thẻ ảnh tự shimmer tới khi **file** của nó vẽ xong (R-336 — đợt chiều chỉ phủ lúc đọc danh sách, không phải chỗ chậm); grip thành ô ngoài cùng bên trái qua `Table.SELECTION_COLUMN` (R-337); tên bác sĩ ở chân tờ "In hóa đơn kèm chẩn đoán" **sửa được trên tờ** (R-338); và `Tạo báo giá` hỏi xác nhận rồi mở **tab "BG 1"** cạnh "Phiếu tư vấn" chứa các dòng đã tick, có ✕ để bỏ (R-339, `useAdviseQuotes` + `AdviseQuoteTabs` + `ConfirmDialog` dùng chung). `consulting-plan.spec.ts` **10/10**, `patient.spec.ts` + `treatment-stage.spec.ts` **65/65**, `treatment-plan` 5/5. Bản báo giá **chưa lưu server** — chưa có aggregate báo giá, ghi ở `unknowns.md`. **2026-09-08 (khuya)** — ba chỗ nữa: ô tên bác sĩ trên tờ in chừa lại khoảng ký (`margin-top`, vì padding trong input chỉ làm ô cao) và nằm giữa cột (R-340); cột grip hẹp lại 28px kèm class cắt padding của antd (R-341); panel `Cột hiển thị` dựng lại theo bản gốc — ✕ đóng, mỗi dòng kéo được, nút `Lưu`, và **thứ tự cột là thứ tự bảng**, không áp dụng gì tới khi bấm Lưu (R-342, cùng khuôn với panel của Kế hoạch điều trị). `consulting-plan.spec.ts` **11/11**; `patient.spec.ts` + `treatment-stage` + `treatment-plan` **81/81**. **2026-09-09** — tab báo giá đo lại: chân `TỔNG KẾ HOẠCH` **vẫn ở đó** (đợt trước ẩn đi là sai), tính theo tick của chính bản báo giá; lệnh giữa đổi thành `Sao chép báo giá` tạo bản mới cùng dòng/cùng tick; bản mới nhất **đứng trước** và ✕ chỉ ở tab đang mở (R-319 → R-321). Trạng thái báo giá dời lên `PatientConsultingTab` để `usePlanVoucher` nhận đúng bộ dòng của tab đang mở. Cột grip nới lên 34px/14px (R-318). `consulting-plan.spec.ts` **11/11**, tổng **81/81** với `patient` + `treatment-stage` + `treatment-plan`. **2026-09-09 (tối)** — sửa lỗi nhân đôi báo giá: `useAdviseQuotes.add` gọi `setQuotes`/`setActiveId` **trong** updater của `setRaised`, mà updater phải thuần khiết — StrictMode gọi hai lần nên mỗi lần tạo chèn hai bản cùng `id`, và cả hai đều vẽ như tab đang mở (R-322). Bộ đếm đổi sang `useRef`; thứ tự tab sắp theo `ordinal` giảm dần (R-323). **Bản build production che loại lỗi này** — StrictMode chỉ gọi hai lần ở dev — nên spec giờ chạy trên **cả** `:8080` **và** `:5173`: **11/11 cả hai**, `patient` + `treatment-stage` + `treatment-plan` **69/69**. Ba locator phải thêm `:visible` vì antd giữ popover/modal đã đóng trong DOM. Báo giá **vẫn chưa lưu server** — chưa có aggregate, xem `unknowns.md`. **2026-09-09 (khuya)** — hai việc server. `Thêm kế hoạch điều trị` **tạo thật** qua `POST patient-treatments` đã có sẵn: accept các dòng còn `Created` rồi mở slip, chỉ đổi tab sau khi server nhận, và voucher chân phiếu đi kèm qua `TreatmentPlan.ApplyVoucher` (trường đã map từ trước mà chưa ai gán) — R-324, kiểm thật thấy `DT02` mang đúng dòng và advise thành `Converted`. Báo giá **đã lưu**: aggregate `PatientQuote` + `bd_patient_quotes` + `api/v1/app/patient-quotes` (list/create/duplicate/update/delete), chỉ lưu tập dòng + thứ tự + tick, **không** lưu giá; số BG do server đánh và chỉ tăng; cách ly chi nhánh 403 — R-325. Cũng sửa được chỗ chặn `dotnet ef migrations add` trong snapshot (một lời gọi `b.Navigation` đứng trước quan hệ tạo ra nó). `consulting-plan.spec.ts` **12/12 trên cả `:8080` và `:5173`**, BE **310 + 556** xanh, FE khác 74/75 (một đỏ `RefundDialog` có từ trước). Not committed |
| F-10 | Lịch hẹn | `VERIFIED` | `e2e/appointment.spec.ts`, `e2e/patient-appointment.spec.ts` | Booking stored and found by a **server-side** search over every appointment; double-booking refused; day and week grids query their own range. The booking dialog is `main`'s (`AppointmentEditorModal` + the three form columns + colour picker + `AppointmentMiniCalendar`), which landed the reference layout, `Color` as a nullable `varchar(20)` and `Notes` on both create and update. This branch adds `initialPatientId` / `initialReason` / `lockPatient` so the patient screen can open it with the patient fixed, and fixes the zod/react-hook-form resolver typing that stopped the file compiling. 3 real full-stack tests on the production build: the tab fills the screen, the dialog's colour and note survive a reload, and an edit updates rather than duplicates. **DIRTY 2026-09-05**: "Lịch sử thay đổi" rebuilt on a real per-change log (`AppointmentChangeLog` + `AppointmentChangeRecorder` on every write of `AppointmentAppService`, migration `20260905000000_AddAppointmentChangeLog`, backfill seeder, `GET appointment-change-log` + `/stats`). Domain 11 / EF 5 / Application 18 tests green; `e2e/appointment-history.spec.ts` written but **not run** (user tests by hand). Needs an API restart for the migration and backfill. **VERIFIED again 2026-09-05** (Level 2): explicit `AppointmentChangeLogController` added, migration applied through DbMigrator, stats adapter fixed to read enum-name keys, null/"" no longer logged as a change; `e2e/appointment-history.spec.ts` 4/4 green on the real stack (booking → Tạo mới row with Web/IP/browser, edit → Cập nhật row + server-side action filter, timeline, empty week with the grey bar and empty card at full height); Domain 13 / Application 7 / Host conventions 14 green. See `features/patient.md` |
| F-11 | Tiếp nhận | `VERIFIED` | `e2e/reception.spec.ts` | Visit stored through the real API; counters served by `/visits/stats` |
| F-12 | CSKH | `VERIFIED` | `e2e/cskh.spec.ts` | Rebuilt 2026-08-26 for **staging.nfcdental.com** parity (new reference standard). 8 real full-stack tests on the production build: URL params (`tab`/`page`/`care_dateMode`/`care_date` with mode anchors), counters from `/stats` + status filtering, per-tab toolbar matrix, Tạo mới + inline note persisted across reload, care-result dialog (Thành công) persisted, Excel download `cskh-dac-biet-*.xlsx`, Phân nhóm CSKH (server search + base-care dialog), branch isolation via claim-scoped list **and a cross-branch create returning 404** (post-security-review guard in `CreateAsync`). See `features/customer-care.md`. **DIRTY 2026-08-27**: grouping `tagId` filter now real (patient `TagIds`) and `taxonomyId` extended to treatment-plan service-group membership — builds/tests green but no runtime retest yet (skipped per user) |
| F-30 | Bệnh án (hồ sơ bệnh án của bệnh nhân) | `VERIFIED` | `e2e/patient-medical-record.spec.ts` | Added 2026-08-31 as the record's second view, behind the header's Chi tiết hồ sơ / Bệnh án switch — the view rides in the URL as `?view=medical-record`. New aggregate `PatientMedicalRecord` + migration `20260831060000_AddPatientMedicalRecord`, an app service, an explicit controller at `api/v1/app/patient-medical-records`, and a new ability subject `patientMedicalRecord`. The index lists the reference's nine forms in its order, wording and colours; the canvas reuses **`MedicalRecordSheet`** — the A4 form Danh mục already draws for "Bệnh án mẫu" — rather than growing a second implementation. 2 real full-stack tests on the production build: the index and bottom bar carry what the reference carries, and a sheet is added, filled, reloaded out of PostgreSQL and removed. **2026-09-09 (R-349…R-358)**: the tab was rebuilt to the reference's own architecture after the owner asked for the sheets to match exactly. The nine forms are **documents, not components** — measured on staging they carry **801 blanks, 155 tick boxes and 17 A4 pages**, which three hand-drawn React sheets were never going to reach. Each form now ships as the blank it is printed on (`medical-record/templates/*.ts`), filled through its `data-medical-record-field` blanks and drawn in its own iframe with the sheet's own stylesheet, measured verbatim (`sheetCss.ts`). A blank that agrees with the record is **not stored**, so a later correction to the patient still shows through — proved by unticking and re-ticking and watching the key appear and vanish from `fieldValues`. Four bugs the rebuild exposed and fixed: tick boxes stored as `""` (`instanceof` cannot cross into the sheet's own document), the sheet drawn blank (values seeded a render too late), sheet names cut mid-word (the row actions sat in flow and left the text 121px), and the default sheet not being the first card. Chrome brought to the reference: no "Xoá phiếu" on the bar, "Đồng bộ phiếu" added (disabled, as it is there), the "Bác sĩ" picker, zoom 60–140% in tens, and "In biểu mẫu" becoming the **"Chọn phiếu in"** popover in Toàn bộ. `patient-medical-record.spec.ts` rewritten to read into the sheet's document: **7/7** on both the production build :8080 and dev :5173, including a spec that counts the blanks, boxes and pages of all nine forms; `treatment-plan.spec.ts` **6/6**. **2026-09-09 chiều (R-359…R-365)**: five things the owner pointed at, plus printing. Section IV-3 of the outpatient form draws its two figures again (the files now ship in `public/medical-record-templates/`). "Số ngoại trú" on the treatment log stopped answering with the patient code — the reference strips that blank's `data-field-source`, since the outpatient number is the clinic's own running number. Rows can be added and removed on the three forms whose tables are really lists: `+` on Phiếu tư vấn/Phiếu theo dõi/Phiếu chăm sóc and `−` on the two logs, clinic-administrator only, positioned and coloured as the reference does. A row is added by cloning and renumbering; removed by dropping it and leaving `custom.deleted-table-row.<hash>` — the reference's own 32-bit hash of the row's first blank — and both survive a save, because a user-added row stores its blanks even when empty. The open sheet's card wears its own form's colour rather than one shared blue (and keeps it while hovered). Printing was broken outright: the legacy `pd-printing` class means "hide every child of body except the portalled `.pd-print-sheet`", which hid the record too. Now `mr-printing`, and the sheet prints where it sits — it is an iframe, so moving it would reload it and lose the typing; hiding the rest by `visibility` alone left a blank first page (the sheet measured y=946px), so `printing.ts` takes the non-sheet branches out of the layout and puts them back on `afterprint`. Sheet then measures (1, 1) at 834×2693. `patient-medical-record.spec.ts` **12/12** and `treatment-plan.spec.ts` **6/6** on the production build :8080; the record suite also 12/12 on dev :5173. Open, in `unknowns.md`: what the Bác sĩ picker fills, whether "có tem" persists (the reference wires no handler either), and the Toàn bộ layout. **Also recorded there: a mis-aimed selector on staging created one sheet on the reference record — a read-only-rule breach, left in place for the owner to decide.** **2026-09-09 chiều muộn (R-366…R-369)**: sheet names were cut mid-word — the card sits in a `<button>` whose `white-space: nowrap` reached the title, leaving `line-clamp: 2` a single line to clamp; the responsive contract was rebuilt to the reference's measurements (breakpoint 1024px; below it the fixed `calc(100dvh - 180px)` height is dropped, the columns fold, the index closes to its header and the page scrolls, with only the paper sliding sideways); the bottom bar swapped its hard-coded `left: calc(50% + 168px)` for the reference's wrapper, so it centres on the sheet column (961 = 961) and wraps to two rows when narrow; and the letterhead no longer prints blank on first paint — the branch name is a request of its own and now takes part in the sheet's `documentKey`. Record suite **14/14** on the production build. **2026-09-09 tối (R-371…R-373)**: the five dated forms (4, 5, 6, 8, 9) carry the date filter the reference gives them, with its own wording — "Ngày tư vấn" on form 5, "Ngày thực hiện" on the rest — read off the reference's own config table rather than guessed; picking a day prints into the form's date blanks and reaches the open document without rebuilding it under the caret; and the sheet column got its scroller back — the reference keeps `h-[calc(100dvh-180px)] min-h-[560px] overflow-y-auto` at every width, where the previous round had handed all the scrolling to the page. The suite also stopped fouling its own data: `openFreshSheet` wipes a form's first copy instead of adding another (122 leftover sheets removed), keyed on the form number so a rename cannot mislead it, and the count spec wipes before counting. Recorded along the way: `PUT { content: null }` means *leave it alone*, not *clear it* — clearing needs an explicit `{"fieldValues":{}}`. Record suite **16/16** on both the production build and dev. **2026-09-09 khuya (R-374…R-376)**: the sheet column was split into the three layers the reference nests — a `relative` column that anchors the floating bar, a bounded scroller inside it, and a `position: sticky` head over the paper — so `Bản NN`, the sheet's name and the date filter stay in place while the A4 moves under them, at both widths. Read off the reference rather than guessed: its own head is `header.sticky.z-10` at 65px with `0 8px 16px -14px rgba(27,42,65,.45)`, and its scroller measures client 720 / scroll 4146. The bar cannot live inside that scroller — an absolute box anchors to a scroll container's padding box and would ride away with the sheet, which is why the reference has two nested divs. The index got a bounded box of its own too, so its list scrolls inside the panel and its header stays put: an addition rather than a copy, since below the breakpoint the reference hides the list outright (panel 70px). And the tick in a card's corner stopped touching the three action buttons — the reference's card is **268x93** (113px on a two-line name) with the tick at 10/10 and an 82x26 button row centred at right 8, a **7.5px** gap; ours collapsed to ~65px whenever the index widened into one column and the meta fitted on one row, bringing the centred buttons up level with the tick. Fixed by holding the reference's own 93px floor and its 11px/16.5px meta line, which lands the card on exactly 93 / 113px. Two deliberate differences recorded: our AntD 6 tick is a 20px box against its 16px (centre matched, so the gap is 5.5px), and the bottom padding below the fold dropped 132px to 24px, since the reserved height only let the page scroll far enough to push the sticky head off the top of the window. The print path was re-verified with print media emulated: the cover starts at y=18 at `zoom: 1` with 7 non-sheet branches out of the layout — the new scroller had to join the `@media print` neutralisation list, or it clips at 678px and prints a single page. Record suite **18/18** on both the production build :8080 and dev :5173, `treatment-plan.spec.ts` **6/6** — 24/24 together. **2026-09-10 (R-377…R-379)**: the `+` that adds a table row could not be clicked on forms 5, 8 and 9. The reference puts `−` on the row's edge but `+` **4px clear of it**, and hiding was driven by the row's `mouseout` — so the element on the far side of that gap is the page, not the button, and the handle vanished from under a hand reaching for it. Hiding now follows where the pointer *is*: the row's box extended 44px to the right, or the button itself. Proved by walking the pointer across in 3px steps on all three forms (rows 27→28, 28→29, 23→24) and by watching the same walk fail on the old code — `locator.click()` cannot catch it, because Playwright jumps the pointer straight onto the button. Printing was cut to a single page with the app's own scrollbar in the preview: the printer paginates what the **root** lays out, and every layer from `.app-shell` down to the sheet column is a fixed-height clipping box, which the isolation hid around but never opened. It now marks the path as well as the hidden branches, and print media opens all of it — measured on the 2312px consent form: **1 page with a 900px root before, 2 pages with a 2312px root after**, sheet at y=0. The marks moved from a class to `data-mr-print`, because `className` is a rendered prop and a React update wrote straight over ours — measured, with the whole index printing above the record. Three new specs, each shown red on the old code first: the pointer walk, printing from the tab, and printing from the "In bệnh án" dialog (four modal layers, plus `zoom: 1` so the preview's zoom stays off the paper). Record suite **21/21** on both the production build and dev, `treatment-plan.spec.ts` **6/6** — 27/27. Also fixed the pre-existing `tsc` break inherited from main: Tiếp nhận's toolbar offers three periods, so its mode is `Exclude<DateNavigatorMode, "year">` rather than a guard at the call site — `npm run build` is green again. Still red and **not** from this round: `reception.spec.ts` waits on `/api/v1/app/visits`, which that board no longer calls (it now reads `/api/v1/app/appointments` and `/appointments/stats`). |
| F-13 | Labo | `DIRTY` | `e2e/labo.spec.ts` | Rebuilt 2026-08-27 as the reference's six sub-routes (`/labo/:section`) after a full read-only survey — see `docs/clone/pages/labo.md` and the Labo section of `docs/clone/api.md`. Khớp cắn / Đường hoàn tất / Kiểu nhịp file under the shared taxonomy the reference uses; Dịch vụ - vật liệu is a group panel beside its material table, with the material hanging off a group rather than a supplier; Nhà cung cấp gained the logo well, contact person, tax code and address the reference collects. Every table is the shared catalog card, `DataTable` and footer pager the Danh mục screens use, and **every search on every tab is a server round-trip**. Mẫu Labo gained the period picker, the patient and doctor filters and Xuất Excel, and lost its create button (the reference raises an order from the patient screen). Five real defects fixed on the way: `labo-suppliers`/`labo-materials` had no HTTP route and answered 404; `SampleFilter` was declared but never applied; `LaboOrder` was missing seven mapped columns; five labo entities carried no `ClinicBranchId`, so a row created in one branch was visible from every other; and every dialog in the app clipped the focus ring under its last field. 12 real full-stack tests on the production build. **Still to build**: the second status dimension (`statusClinic`), the returned-files column, the order detail modal and its print sheet — see `docs/clone/pages/labo.md` §9. **2026-09-08 — patient Labo tab, child orders**: the tab's dialog is now the reference's three pills (Đặt mới / Làm tiếp công đoạn / Bảo hành) driven by `?laboModal=…&laboRowId=…`; Đặt mới from the tab picks the plan and the open service line itself; the two child tabs pick a parent order, lock its code and facts, offer "Theo vật liệu cũ" (server copies the parent's material, refuses with `Labo:0011` when it has none) or "Thay đổi vật liệu mới". BE: `LaboOrder.CreateChild`, `ParentOrderId`, `Kind`/`ParentOrderId` on the create DTO, unique code only among parents, plan/service facts on the DTO (migration `20260908090000_AddLaboOrderParent`). `e2e/labo-warranty.spec.ts` 2/2 real stack on the production build (`vite preview` :8083) plus the stage-dialog Tạo Labo test; Domain 7/7, EF 6/6, Application 32/32. R-312 (2026-09-08): a code taken between opening the dialog and Lưu is swapped for the next free one server-side, numbering runs across branches to match the table-wide unique index; `labo-warranty` 2/2, `labo` 14/14, stage Tạo Labo 1/1. R-318 (2026-09-08): Xem chi tiết modal + PHIẾU ĐẶT HÀNG LABO print sheet built on the patient tab (`e2e/labo-detail.spec.ts` 1/1 on a preview against the owner's host). Still deferred: `statusClinic`. Uncommitted R-315 (create + pictures in one multipart request) verified on a scratch host; R-316 (child form names the line's service and the labo service) has contract tests only — owner retests both (2026-09-08). |
| F-14 | Vật tư | `VERIFIED` | `e2e/materials.spec.ts` | Rebuilt 2026-08-26 as the reference's three sections (`/materials/clinic`, `/allocation`, `/department`). Materials file under `supplies` taxonomy groups through the panel Danh mục shares; a material and its group survive a reload; a department is created and survives a reload; the allocation list is real. Two app services that had no HTTP route (`departments`, `material-allocations`) were returning 404 to the browser and now have controllers. `InventoryItemDto.Status` was mapping to the enum's unnamed zero on every row and `TaxonomyName` was never filled — both are now stamped from the entity. 4 tests, run against the production build. See `docs/clone/pages/materials.md` for what the empty reference branch left UNKNOWN |
| F-15 | Quản trị vận hành | `VERIFIED` | `e2e/operations.spec.ts` | Article draft → published; task lifecycle; department travels with the query |
| F-16 | Công cụ (call/message/Zalo/hóa đơn) | `BLOCKED` | — | `UNKNOWN_REFERENCE_BEHAVIOR` — no data on the reference to observe |
| F-29 | Danh mục — màn hình phẳng (Đơn thuốc mẫu, Thẻ hồ sơ, Phương thức thanh toán) | `VERIFIED` | `e2e/taxonomy-flat.spec.ts` | Added 2026-08-24. Đơn thuốc mẫu drops the group panel and the group column while Bệnh án mẫu keeps both; a coloured thẻ hồ sơ is stored and survives a reload; MoMo and bank accounts stay on their own tabs across a reload; an account with no holder is refused. New tables `bd_patient_tags` (branch-scoped) and `bd_payment_accounts` |
| F-31 | Danh mục — ảnh QR phương thức thanh toán | `VERIFIED` | `e2e/payment-qr.spec.ts` | Added 2026-08-24. "Tải ảnh QR" uploads through a real multipart POST to MinIO, the saved QR is served back by the API and survives a reload, removal clears both row and blob, and a non-image file is refused before it leaves the browser. New columns on `bd_payment_accounts` (`QrImage*`); the reference's own limits could not be observed — see `docs/clone/unknowns.md` Re-verified 2026-08-25 after the rebase onto `main` (Ant Design 6, Tailwind removed) — the catalog UI was rebuilt on antd + a named stylesheet, and the suite was run against the **production build** (`vite preview`), not the dev server: see R-69…R-76 in `03-regression-log.md`. **Chốt 2026-08-25 — màn hình Danh mục coi như hoàn thiện; xem mục 17 của CLAUDE.md trước khi sửa.** |
| F-32 | Danh mục — nhóm phân loại (dialog, tìm kiếm, kéo-thả) | `VERIFIED` | `e2e/taxonomy-groups.spec.ts`, `e2e/taxonomy.spec.ts` | Added 2026-08-24. `Mức độ ưu tiên` is stored and decides the order, and comes back into the dialog on edit; the search sends `filter=` to the API and empties the panel on a term the server cannot match; search is trimmed, case-insensitive and term-wise across every column shown (`TRÁM RĂNG SỨ`, `   trám răng sứ   ` and `sứ trám` all find the same row; whitespace alone is not a search); a real pointer drag reorders the panel as it passes rows and saves the whole order in **one** `POST /taxonomies/reorder`; the entry table does the same through `POST /catalog-entries/reorder`, with the lifted row following the pointer sideways as well as down Re-verified 2026-08-25 after the rebase onto `main` (Ant Design 6, Tailwind removed) — the catalog UI was rebuilt on antd + a named stylesheet, and the suite was run against the **production build** (`vite preview`), not the dev server: see R-69…R-76 in `03-regression-log.md`. **Chốt 2026-08-25 — màn hình Danh mục coi như hoàn thiện; xem mục 17 của CLAUDE.md trước khi sửa.** |
| F-33 | Danh mục — parity dialog (P1+P2) | `VERIFIED` | `e2e/taxonomy-groups.spec.ts` | Added 2026-08-25. Khung dialog dùng chung theo bản gốc (tiêu đề + kẻ, floating label, một nút `Lưu`, không `Huỷ`); menu hàng nhóm còn `Chỉnh sửa`/`Xoá` với grip nhận phím `↑`/`↓`; ba tab đơn giản dùng đúng bộ field của bản gốc và `Đã xoá` xoá bản ghi khi lưu; tab Nghề nghiệp không có nút `Xuất`. P3–P7 còn lại — xem `save/taxonomy-parity-plan.md` Re-verified 2026-08-25 after the rebase onto `main` (Ant Design 6, Tailwind removed) — the catalog UI was rebuilt on antd + a named stylesheet, and the suite was run against the **production build** (`vite preview`), not the dev server: see R-69…R-76 in `03-regression-log.md`. **Chốt 2026-08-25 — màn hình Danh mục coi như hoàn thiện; xem mục 17 của CLAUDE.md trước khi sửa.** |
| F-34 | Danh mục — dialog theo từng danh mục (P3–P7) | `VERIFIED` | `e2e/taxonomy-dialogs.spec.ts` | Added 2026-08-25. Dịch vụ giữ đủ cấu hình giá/thuế, công đoạn và bảo hành, và hai ô tính ra (`Giá sau giảm` 900, `Thực thu` 990 từ giá 1.000 giảm 10% cộng VAT 10%) trả về từ server; thuốc giữ hoạt chất và cả hai giá; chẩn đoán giữ nội dung rich-text và ghi chú; đơn thuốc mẫu lưu dòng thuốc và tự tính `Số lượng`; bệnh án mẫu lưu tờ A4 đã điền. Tờ A4 dựng lại 2026-08-25 theo đúng bản gốc — 3 trang, 17 ô nhập, test khẳng định cả tiêu đề in sẵn lẫn ô nhập trên cả ba trang. 4 bảng mới + 3 cột trên `bd_catalog_entries` Re-verified 2026-08-25 after the rebase onto `main` (Ant Design 6, Tailwind removed) — the catalog UI was rebuilt on antd + a named stylesheet, and the suite was run against the **production build** (`vite preview`), not the dev server: see R-69…R-76 in `03-regression-log.md`. **Chốt 2026-08-25 — màn hình Danh mục coi như hoàn thiện; xem mục 17 của CLAUDE.md trước khi sửa.** |
| F-35 | Vận hành — phân loại và bài viết theo khối | `VERIFIED` | `e2e/operations.spec.ts` | Added 2026-08-25. Rà soát lại toàn bộ theo bản gốc cùng ngày (R-93→R-98): tám khối là tám route, **mỗi khối một tham số sub-tab riêng** và chúng cộng dồn trong URL nên rời đi rồi quay lại vẫn đúng tab; sub-tab lấy đúng từng khối (Khối bảo vệ 3, Khối tài chính 6 tab riêng); Khối điều trị/tài chính có thêm hàng tab giữa Tổng quan/Truy cập. **Chỉ Trang chủ/Quy trình/Công việc là màn bài viết** — 6 sub-tab còn lại là báo cáo, render `OperationReportPanel` chứ không giả vờ là màn bài viết; cột của chúng ghi ở `docs/clone/pages/operations.md`. Panel phân loại dựng riêng theo bản gốc (không tiêu đề/số đếm/mô tả/tìm kiếm/kéo-thả, hành động hiện khi hover). Hai dialog theo đúng chữ và số đo đọc từ DOM. Ảnh trong bài viết lưu ra blob, nội dung chỉ giữ link. Báo cáo và nút xoá của bản gốc còn `UNKNOWN` — xem `docs/clone/unknowns.md` |
| F-36 | Vận hành — bảy màn báo cáo | `VERIFIED` | `e2e/operations-reports.spec.ts`, `OperationsReportWindowTests` | Added 2026-08-26. Sáu endpoint chỉ-đọc (`/operations/reports/...`), lọc theo chi nhánh và theo kỳ Ngày/Tuần/Tháng/Năm với ngày mốc được server làm tròn về kỳ. Báo cáo gộp 5 nguồn thành một nhật ký và lọc theo hành động; Chẩn đoán chưa điều trị đọc `HasTreatmentService = false`; Khách hàng phát sinh gộp theo nhân sự tư vấn, tách khách mới/cũ theo việc phòng khám đã tư vấn người đó trước kỳ hay chưa; Hóa đơn; Hoàn thành theo dịch vụ với 5 thẻ số (gồm % so với kỳ trước); Truy cập dùng chung cho Khối điều trị và Khối tài chính, 3 thẻ số kiêm luôn bộ lọc. Đơn thuốc giữ đúng câu của bản gốc vì bản gốc cũng chưa dựng. Dữ liệu demo: `BlueDentalReportsDemoSeeder` rải 120 ca trên 75 ngày — xem R-99→R-103 về hai lỗi seeder phải sửa trước. Công thức các thẻ số của bản gốc còn `UNKNOWN` — xem `docs/clone/pages/operations.md` |
| F-37 | Chăm sóc KH — tab trong hồ sơ bệnh nhân | `VERIFIED` | `e2e/patient-care.spec.ts` (2), `CustomerCareAppServiceContractTests` | Rebuilt 2026-09-05 to the staging reference after a read-only survey and the owner's answers (see `docs/clone/pages/patient-detail.md`, Tab 8, and `features/customer-care.md`). Eight single-select counters (Đã chăm sóc · Tốt · Khá · Bình thường · Khiếu nại | Đặc biệt · Định kỳ · Cơ bản) each mapping to one server-side param (`status` / `outcome` / `type`), `+ CSKH đặc biệt`, the nine reference columns with a Chi tiết link → "Chi tiết phiếu", Chỉnh sửa → "Cập nhật chăm sóc khách hàng" and Xoá → "Xóa lượt chăm sóc". Backend: `Outcome` filter on the list, per-type stats (`special` / `periodic` / `base`), `outcome` accepted on update, `DELETE care-records/{id}` (soft delete, Manage permission, branch guard). Real-stack spec on the production build: create with Tốt → row + three counters move → Khiếu nại chip hides it, Tốt chip shows it, second click clears → detail dialog → edit to Khiếu nại (PUT) survives a reload → delete (DELETE) survives a reload and the counter returns to baseline. Deviations: house chrome and the shared 440px confirm dialog |
| F-17 | Báo cáo doanh số | `DIRTY` | `e2e/report.spec.ts` | 2026-09-04: `/report` dựng lại theo bản gốc, FE-only trên mock data (`reportMockQueries.ts`); mọi nút ghi chỉ hiện toast demo. `e2e/report.spec.ts` viết lại cùng ngày (4 test xanh trên `vite preview` :8080, đăng nhập thật, khẳng định không có POST/PUT/PATCH/DELETE nào tới `/api/`) — nhưng chỉ chứng minh UI + không ghi, **không** phải bằng chứng runtime vì dữ liệu là mock. `/clinic-reports` không còn được UI gọi. Retest Level 2 khi nối BE. 2026-09-04 (chiều): Xuất Excel sub-tab Thanh toán dựng lại theo file gốc — 15 cột đúng thứ tự (tách mã/tên khách, thêm Người tạo, Mã phiếu điều trị, Chi nhánh, Thông tin thanh toán, Ghi chú; "Tổng tạm ứng còn lại" đứng trước "Thực thu"), số tiền ghi ô số thô; `report.spec.ts` mở file tải về và so header + kiểu ô. Xem `docs/clone/pages/report.md` |
| F-18 | Kết quả kinh doanh | `DIRTY` | `e2e/report.spec.ts` | 2026-09-04: cùng đợt dựng lại với F-17 — `BusinessResultTab` đọc `useMockBusinessResult`; 6 dòng theo đúng thứ tự bản gốc, chưa có BE |
| F-19 | Công đoạn điều trị | `VERIFIED` | `e2e/treatment-stage.spec.ts` | Chưa làm → Đang làm → Hoàn thành, persisted. **Model is BlueDental's assumption** — see below |
| F-20 | Phân tách chi nhánh | `VERIFIED` | `e2e/branch-isolation.spec.ts` | Rewritten 2026-08-24 for the stricter contract: naming a branch the account may not work in answers **403** rather than being silently narrowed; `manager` (no assignment) reaches both branches; an unqualified list still narrows to the caller's own |
| F-30 | Chuyển chi nhánh trên header | `VERIFIED` | `e2e/branch-switcher.spec.ts` | Added 2026-08-24, extended the same day. The switcher lists only branches the account may work in; a branch-scoped account neither sees the other branch's catalog nor writes into it; a clinic-wide account (`manager`) switches between both branches and each shows its own seeded catalog, persisted across a reload. The popover now closes on select |
| F-21 | Phiếu điều trị + dòng dịch vụ (tab Kế hoạch điều trị rebuilt 2026-09-07) | `VERIFIED` | `e2e/treatment-plan.spec.ts` | Tạo kế hoạch mới (service picker, dentist, diagnosis, tooth chart) saves a slip; money columns, Đã tạo pill, shared pager; eye / + / Phiếu thu / Xem tất cả dịch vụ modals; Cột hiển thị in memory only; ≤640px record cards; branch-2 account gets 403. 2026-09-07 (chiều): the DT code link, the summary-card items and the ≤640 card head now navigate to the detail page (F-39); `treatment-plan.spec.ts` re-run green |
| F-22 | Thanh toán bệnh nhân | `VERIFIED` | `e2e/treatment-plan.spec.ts` | Only finished work becomes Phải thu; refund capped at what was collected; prepaid held, not counted as paid |
| F-23 | Đơn thuốc | `VERIFIED` | `e2e/prescription.spec.ts` (5), `PrescriptionTests`, `PrescriptionAppServiceContractTests`, `PrescriptionMappingTests` | **Rebuilt 2026-09-05** to the staging reference and the owner's answers (see `docs/clone/pages/patient-detail.md`, Tab 7, and `features/prescription.md`). Tab = right-aligned "Tạo đơn thuốc" over Mã đơn thuốc · Bác sĩ · Chẩn đoán · Tái khám · Ngày tạo · Thao tác (Sửa/Xóa), "Hiển thị n trên m" pager even when empty; the dialog rides in the URL as `create=true`, shows the patient block, template picker, doctor (required), diagnosis, lời dặn, "Lưu đơn thuốc mẫu" → "Tên đơn thuốc mẫu", Điều trị, Tái khám and the medicine lines (Số lượng = Ngày uống × Mỗi lần × Số ngày, derived). Lines rebuilt as `bd_prescription_items` with timesPerDay/amountPerTime/days/usage (migration `20260905120000_RebuildPrescriptionLines`); saving with the box ticked files a `prescription_template` catalog entry under the typed name, and picking it back prefills lines + lời dặn. Real-stack spec: create with lines + template → reload → template offered and prefilled; Sửa persists (PUT); branch2 gets **403** on the slip; Xóa confirms then DELETEs and the row is gone after reload. The line editor was lifted into `src/components/prescription-lines/` and the Đơn thuốc mẫu dialog now uses it — the taxonomy/payment-qr/branch suite re-run on the production build is 42 specs: 39 green, 3 red that fail identically on a clean HEAD `20c4815` build (see the 2026-09-05 regression-log entry). Earlier note (2026-08-28): the template picker used to call a route that did not exist |
| F-24 | Hình ảnh bệnh nhân | `VERIFIED` | `e2e/patient-image.spec.ts` | Real multipart upload to MinIO, bytes fetched back through the API. **2026-09-05**: tab rebuilt to the reference — `type` (Trước/Sau điều trị) filter, day timeline, `ordering` with `PUT /reorder`, in-house viewer, confirm-then-delete. The spec uploads two pictures, filters, views, drags one past the other and reloads, deletes both, and checks `branch2` gets **403** on branch one. `current-user` now returns the granted permissions, which the tab's upload / sort / delete buttons depend on. Same day: viewer zoom by wheel / double-click, drag-to-pan, palette with a custom colour, strokes riding inside the picture frame; all exercised in the viewer test |
| F-25 | Nhân viên | `VERIFIED` | `e2e/staff.spec.ts` | Account created, edited and deleted; weak password refused by Identity |
| F-28 | Tìm kiếm nhanh (Ctrl K) | `VERIFIED` | manual browser run, 2026-08-24 | The palette used to render a search box with no `value`, no `onChange` and no results. It now queries `/patients?filter=` and opens the record; typing TRAN returned 8 real patients and the first one opened `/patient/:id` |
| F-27 | Thanh toán & hoá đơn (màn hoá đơn phòng khám) | `VERIFIED` | manual browser run, 2026-08-24 | Payment recorded through the real modal on `HD-202608-0012`; PostgreSQL shows `paid_amount=1000000`, `Status=3 (PartiallyPaid)`; survives a reload. Excel export returns a real `.xlsx` |
| F-26 | Song ngữ Việt/Anh (i18n) | `VERIFIED` | manual browser sweep, 2026-08-23 | Switch is instant, no reload; 985 keys, 0 untranslated; survives reload via `localStorage`; Zod messages and `Accept-Language` follow the switch |
| F-38 | Hồ sơ bệnh nhân — thẻ hồ sơ, lý do đến khám, hàng tiền, bảng điều trị | `VERIFIED` | `e2e/patient.spec.ts` (3 mới), `PatientTests`, `PatientMappingTests` | Ba lệch chủ dự án chỉ ra, sửa 2026-09-06 sau khảo sát chỉ đọc trên staging (`docs/clone/pages/patient-detail.md`, Tab 1). (1) **Tag**: dấu ✓ trong picker vẽ trắng-trên-trắng vì `CheckOutlined` ra `<span>` trần, trúng rule chip của hàng — chip tách thành `.pd-tag-chip`; nhãn của hồ sơ nay hiện cạnh tên theo thứ tự danh mục, cùng hàng `flex-wrap` với tên + bút chì, chọn được nhiều tag, lưu ngay. (2) **Lý do đến khám** đổi từ một chuỗi sang danh sách có ngày: entity con `PatientExaminationReason` + bảng `bd_patient_examination_reasons` (migration `20260906000000_AddPatientExaminationReasons` mang giá trị cũ sang làm dòng gốc), `POST patients/{id}/examination-reasons` cho nút **+** (thêm dòng), dialog "Chỉnh sửa hồ sơ" vẫn sửa dòng gốc tại chỗ — đúng như bản gốc. (3) **Tạm ứng**: hàng tiền lên 7 ô, đọc `payment.prepaid` đã có sẵn. Real-stack trên bản build production: thêm hai lý do → in ngày `dd/MM/yyyy` cột 88px + nội dung đỏ, mới-trước, sống qua reload; bật một tag → tích trong picker + chip cạnh tên, sống qua reload; 7 ô tiền trên một hàng, `Tạm ứng` khớp con số server trả. `e2e/patient*.spec.ts` 41/41 xanh, `branch-isolation` 5/5. **Đợt 2 cùng ngày** (R-183…R-187): lý do đến khám hạ về 13.5px/20 cho khớp nấc chữ của cột; filter "Phân loại theo Tag" bỏ chip màu bịa, trả về dòng chữ thuần như "Phân loại dịch vụ" (bỏ luôn khỏi `SearchSelect`, kéo theo Phân nhóm CSKH); thẻ Lịch hẹn gần nhất có combobox đổi bác sĩ ngay tại chỗ — PUT gửi nguyên lịch hẹn vì `UpdateAsync` dựng lại slot + details từ request; bảng điều trị dựng lại đủ **10** cột theo số đo bản gốc (th sentence-case 14px, ô kẻ 12/16, chip mã DT + trạng thái, chip răng, "Phụ tá:", nút + xanh cho công đoạn, cột "Chăm sóc sau điều trị") và Thao tác mở **"Tạo phiếu thanh toán"** 1024px — mỗi dòng dịch vụ một `POST patient-payments` mang `treatmentServiceId`, nên "Còn nợ" của đúng dòng đó mới đổi. BE thêm `stageNotes` / `paidAmount` / `outstandingAmount` / `afterCareStatus` lên `TreatmentServiceDto`. `e2e/patient*.spec.ts` **50/50**, Domain 250 / Application 516 / EF 51 xanh, `treatment-plan` 4/4, `treatment-stage` 2/2. **Đợt 3 cùng ngày** (R-188…R-192): nút cột Công đoạn mở **"Chi tiết phiếu"** dựng theo bản gốc (2 tab THÊM/TIẾP TỤC CÔNG ĐOẠN có badge đếm, Thanh toán + In lịch sử điều trị, 4 tiêu đề cột, form Ngày tạo/Dịch vụ disabled + Bác sĩ/Phụ tá/Bác sĩ hỗ trợ + chip răng + Nội dung điều trị, bảng LỊCH SỬ ĐIỀU TRỊ kẻ ô với Hoàn thành / Tải ảnh / Tạo Labo) — Lưu gọi `POST treatment-stages` thật; modal thanh toán soi lại từng field (tiêu đề section viết hoa + icon, hàng fact 150px canh trái, pill Còn nợ, lỗi hiện ngay, radio chia tiền, nhãn nổi + 0/500, phương thức là pill bo tròn có badge icon) và bỏ ô "Số tiền của phiếu" tự bịa; thêm **Ví momo** — `PaymentMethodKind.EWallet = 5` cùng `ByEWallet`/`RefundByEWallet` trên báo cáo phòng khám để tiền ví không rơi khỏi tổng; `Còn lại` tính sống theo số đang nhập; Chia Tiền Thủ Công ra một ô cho mỗi dịch vụ đã tích, mồi sẵn Còn nợ. `UploadPatientImageInput` thêm `treatmentStageId`; `useStageMutation` invalidate thêm `patient-treatments`. e2e **67/67** (`patient*`, `treatment-plan`, `treatment-stage`, `report`, `branch-isolation`). **Đợt 4 cùng ngày** (R-193…R-196), đọc hợp đồng API từ **bundle** của bản gốc (static asset — rule 00 cho phép) nên **không ghi một byte nào** lên staging; kiểm trước thì thấy tài khoản khảo sát không có ability `payment` (403) và `treatmentStage` không có `delete`, tức là ghi vào cũng không hoàn tác được. Thu được: `/v1/payment-v2` (create · PATCH · void · finalize · export) với schema Joi đầy đủ, và `/v1/patient-stages` (create · continue · updateStatus · revertStatus · stage-service-items). Áp dụng: **`paymentAccountId`** bắt buộc khi chọn Ngân hàng / Ví momo — bảng chọn tài khoản đọc từ danh mục `PaymentAccount`, cột mới `PatientPayment.PaymentAccountId` (migration `20260906120000_...`) và guard ở aggregate; ô tiền Tự động **mồi sẵn** theo công thức của bản gốc (Dư nợ bị chặn ở số đang giữ); câu báo vượt quá dùng đúng chữ bản gốc; form công đoạn bắt buộc bác sĩ + răng + nội dung (max 1000). **Đợt 5 cùng ngày** (R-197…R-200) đóng nốt lệch cuối: một phiếu thu mang **nhiều** dịch vụ, đúng như bản gốc. `PatientPayment` có bảng con `PatientPaymentLine` (`treatmentServiceId`, `amount`) và cột `SplitMode`; bỏ `PatientPayment.TreatmentServiceId` (migration `20260906140000_AddPatientPaymentLines` backfill mỗi phiếu cũ thành một dòng rồi mới drop cột). Chia tiền chuyển hẳn về server: **Tự động** chỉ gửi tổng, server rải từ dòng cũ nhất và chặn ở đúng Còn nợ từng dòng; **Thủ công** gửi `items[]`. Aggregate từ chối dòng bằng 0 và phiếu có tổng các dòng lệch tổng phiếu (`BlueDental:Billing:0091`); rollup `paidAmount` đọc theo dòng phiếu nên hoàn tiền vẫn trừ đúng dòng. Vẫn **không ghi gì lên bản gốc** — xác minh bằng dữ liệu local. Real-stack: dựng một phiếu hai dòng bằng chính API của app, thu 3.000.000 trên 4.900.000 → **một** POST mang cả hai `treatmentServiceIds`, `splitMode: 1`, không `items`; đọc lại đúng một phiếu, dòng 2.400.000 + 600.000, `paid 2.400.000 / due 0` và `paid 600.000 / due 1.900.000`, lịch sử hiện **một** dòng. Ba spec đỏ sau đổi schema là do dữ liệu demo nay có phiếu nhiều dòng nên "dòng đầu bảng" ≠ "dòng vừa bấm" — đã sửa cho độc lập thứ tự (bấm theo `tr[data-row-key]` của dòng còn nợ, cộng Còn nợ theo các dòng đang tích, đọc phiếu theo id dịch vụ chứ không theo thứ tự dòng). e2e **49/49** (`patient`, `patient-images`, `treatment-plan`, `treatment-stage`, `report`, `branch-isolation`), Domain **262** / Application **516** / EF **51** xanh. **Đợt 6 cùng ngày** (R-201…R-214) rà lại "Chi tiết phiếu" theo từng nút, từng modal ẩn, vẫn **không ghi gì lên bản gốc** (mở dialog rồi đóng, đọc markup và bốn GET dialog tự gọi): form công đoạn kín cột (đo 366px = đúng bề rộng cột ở 1600) và xuống 2 cột trước khi xuống 1, răng thành chip xanh đặc, `Hình ảnh:`/`(Trống)` tách hai dòng, bỏ dấu sao ở nhãn ghi chú, `Tải Ảnh` trong form bật sẵn (ảnh chọn trước được đính sau khi lưu); LỊCH SỬ ĐIỀU TRỊ gộp theo ngày với ô Ngày trải hết ngày đó, in `d/M/yyyy`, ảnh công đoạn thành ô 68px mở viewer, bút chì sửa ghi chú tại chỗ qua `PUT treatment-stages/{id}`, và **Phụ tá / Bác sĩ hỗ trợ được lưu thật** (`TreatmentStage.SubStaffId` mới — migration `20260906160000_AddStageSubStaff` — cùng `subStaffName`/`secondStaffName`); ba hành vi ẩn dựng đúng bản gốc: **In lịch sử điều trị** mở modal in kèm tờ A4 ẩn có hai ô ký, **Thanh toán** rời dialog sang Kế hoạch điều trị, **Tạo Labo** mở dialog **"Đặt mới"** mồi sẵn từ công đoạn (`LaboOrder` thêm `ToothShade`/`Quantity`/`TreatmentServiceId`/`TreatmentStageId` — migration `20260906170000_AddLaboOrderTreatmentLink` — và `GET labo-orders/next-code` sinh `LABO-yyyyMMddN`); dòng dịch vụ đã hoàn thành đổi sang chip xám không bấm được, khớp bộ lọc `status=created,inProgress,guarantee` của bản gốc. e2e **69/69** (thêm `labo`), Domain **264** / Application **516** / EF **51** xanh. **Đợt 7 cùng ngày** (R-215…R-220), vẫn chỉ đọc bản gốc: bảng điều trị đổi sang **một dòng cho mỗi công đoạn** đúng như `/v1/patient-timeline` (`type: "stage"`), ô Ngày trải hết ngày bằng `rowSpan` và tính lại span sau khi lọc, ô Công đoạn rút về hai trạng thái (+ hoặc chip xám) vì con số của bản gốc là checklist danh mục chứ không phải số công đoạn; cờ `disabled` của bản gốc được dựng lại — chỉ công đoạn **mới nhất** của một dịch vụ còn sống, cái cũ `aria-disabled` + mờ + `pointer-events: none`, ô Hoàn thành khoá và mất nút Tạo Labo; dialog Labo **"Đặt mới"** dựng lại bằng chính field component của source (`SearchSelect` trong `FloatingLabel`, `FloatingLabel` nay có `required` in dấu sao đỏ trong nhãn — 11 field bắt buộc, khớp bản gốc) theo đúng thứ tự khối, hai dải chip có pill viền đứt khi rỗng, hàng `Răng:*` có "Chọn tất cả", và ô vuông 80px Tải ảnh. e2e **72/72**. **Đợt 8 cùng ngày** (R-221…R-228), vẫn chỉ đọc bản gốc: ô Công đoạn suy từ **chính công đoạn** của dòng chứ không từ dòng dịch vụ, và có ba trạng thái — + xanh (chưa xong), chip hổ phách **Bảo hành** (đã xong, dịch vụ có kỳ bảo hành), chip xám "Không bảo hành"; thêm công đoạn mới **không** đóng các công đoạn cũ (cờ `disabled` của bản gốc chỉ làm mờ dòng trong dialog, ngoài bảng mọi công đoạn chưa xong vẫn bấm được); dialog **"Tạo bảo hành"** dựng theo bản gốc và ghi một công đoạn `isGuarantee` (cột mới `TreatmentStage.IsGuarantee`, migration `20260906180000_AddStageGuarantee`; `TreatmentServiceDto.WarrantyDays` đọc từ `CatalogServiceConfig`, seed demo có cả dịch vụ có và không có bảo hành); ba bước **Tiếp nhận** nay bấm được lần lượt qua `check-in`/`start`/`complete` và in giờ thật; **"Tạo tái khám"** liệt kê công đoạn đã hoàn thành. Kèm một lỗi seed do đợt R-193 để lại: phiếu ngân hàng demo không kèm tài khoản nên aggregate từ chối — chỉ lộ khi chạy migrator ở Development. e2e **78/78** (thêm `appointment`), Domain **264** / Application **516** / EF **51** xanh **Đợt 9 — 2026-09-07** (R-229…R-241), chủ dự án đưa tài khoản staging và yêu cầu mở bản gốc lên đo; vẫn **chỉ đọc** (đăng nhập, mở trang, mở dialog, đọc `getComputedStyle`/`getBoundingClientRect`) và **không đính file** vào form Labo của bản gốc vì chọn file có thể upload ngay. Chạy lại **cả** bộ đã lộ 4 spec đỏ mà lần trước ghi 78/78: hai lỗi spec (hàm đọc `Còn lại` ăn mất dấu trừ nên phép hiệu sai khi gõ quá số còn nợ; spec đổi bác sĩ chọn người đã kín giờ nên ăn 409), một nhiễm dữ liệu (`warrantable` không lọc trạng thái dòng), và **hai lỗi thật**: ô Ngày của bảng điều trị **mất/trải lố khi sang trang 2** vì gộp ngày chạy trước khi cắt trang (bất biến "tổng rowSpan = số dòng" đo được 20≠23 — nay gộp sau khi cắt), và `rowKey="id"` **trùng khoá** vì một dòng là một công đoạn (nay `${row.id}:${row.stageId}`, vẫn mang tiền tố id dịch vụ). Thêm một lỗi **dữ liệu seed**: seeder chỉ giữ chỗ theo (bác sĩ, slot) còn bệnh nhân bốc ngẫu nhiên sau đó, nên **206 cặp lịch hẹn trùng giờ trên 5 bệnh nhân** — đúng cái `HasPatientConflictAsync` chặn khi ghi, khiến thẻ lịch hẹn của họ không sửa được (lỗi 0006); nay giữ chỗ cả hai chiều và ca dài giữ đủ 2 slot (**chỉ có tác dụng khi seed lại**, chưa chạy trên DB mới). Soi bản gốc còn sửa 5 chỗ giao diện của "Đặt mới": dialog **772px** (trước 880) với hai cột 349px và gap 20px — đo lại lệch 1–4px ở mọi mục; nhãn field về một slate `#5a6b82` cho ba trạng thái (nghỉ 80%, nổi 100%, focus `#2671D8`); ô Tải ảnh dashed `#B9C4D4` chữ xanh hover đổ nền `#E7F0FB`; pill viền đứt `#DCE3EE` và **canh giữa** khi rỗng; và **`.pd-labo-grid` khai hai lần trong cùng file** — bản 2 cột đè bản 3 cột nên dialog "Tạo phiếu Labo" 1180px bị bóp, đã thu hẹp rule mới vào `.pd-labo-dialog`. Cùng đó vá rò rỉ `URL.createObjectURL` gọi thẳng trong JSX (mỗi ký tự gõ sinh thêm một blob URL cho mỗi ảnh nháp, không bao giờ revoke) và mở lại spec Tiếp nhận vốn tự **skip** vì thẻ hiện lịch hẹn đã đến — nay tìm bệnh nhân chỉ có một lịch hẹn còn sống nên chạy thật. e2e **84/84** xanh, **0 skip**, chạy **hai lượt liên tiếp** (trước đợt này: 79 xanh / 4 đỏ / 1 skip) trên `patient`, `patient-image`, `treatment-plan`, `treatment-stage`, `labo`, `report`, `branch-isolation`, `appointment`; Domain **264** / Application **516** / EF **51** xanh. Còn treo, đã ghi `unknowns.md`: ảnh nháp của bản gốc không quan sát được (phải ghi mới thấy; cả 20 phiếu Labo staging đều rỗng ảnh) nên ta lấy **80×80** cho bằng ô Tải ảnh đo được; **kính lúp** "Tìm dịch vụ" cạnh hai dải chip chưa dựng; ô Công đoạn khi dòng ở trạng thái **"Chuyển đổi"** bản gốc vẽ `+` **disabled** mà ta chưa mô hình hoá; và thẻ "Lịch hẹn gần nhất" chọn phía client trên trang 50 dòng nên có thể hiện sai lịch hẹn với bệnh nhân có hàng trăm phiếu. **Đợt 10 — 2026-09-07** (R-258…R-273): cột **Dịch vụ** của danh sách bệnh nhân trống vì `PatientAppService` không eager-load `Services` (`WithDetailsAsync`), và cột này cùng **Bác sĩ** nay lấy **dòng mới nhất** chứ không gộp mọi dịch vụ; **Hoàn thành bỏ tick được** (`/revert-status`, `TreatmentStage.Revert()`), và `MoveServiceLineAsync` chạy **cả hai chiều** nên dòng dịch vụ và phiếu mở lại theo; **tái khám là một row riêng** — aggregate `PatientReExamination` + bảng `bd_patient_re_examinations` + endpoint `patient-re-examinations`, mã `REX{n:D3}`, chip **xám**, ô Công đoạn/Chăm sóc để trống, không dòng Phụ tá (lượt đầu mô hình **sai** thành cờ `isReExamination` trên công đoạn, đã rollback migration đã áp); form tái khám **chọn răng** (toggle, không tick sẵn) và **list ảnh** thumbnail có nút xoá; validate chuyển từ toast sang **error text dưới từng field**, báo hết một lượt và tự xoá khi sửa; form **Tiếp tục công đoạn** dùng chung `StageShots` nên cũng list ảnh; **bước 3 Tiếp nhận** trước đây chưa bao giờ bấm được (`/complete` bind `[FromBody]`, client không gửi body) và ba bước nay **ba màu**; nút **Tái Khám** / **Chi Tiết** mở hai dialog đúng bản gốc (`StageFollowUpDialog`, `ServiceDetailDialog` mới). Kèm **một lỗi có sẵn từ trước** lộ ra nhờ spec: bảng điều trị gọi `maxResultCount: 200` trong khi server sắp theo `(TreatmentServiceId, SequenceNumber)`, nên trên bệnh nhân quá 200 dòng thì công đoạn vừa thêm **biến mất khỏi mọi trang** — nâng lên 1000, giới hạn còn lại ghi ở `unknowns.md` (R-273). Spec thôi khẳng định trên **một trang**: đếm qua tổng bảng tự báo (một trang đã đầy không thể hiện việc thêm dòng), tra dòng thì đi lần lượt các trang theo `serviceId:stageId` — cách này bắt được một spec đang **xanh vì lý do sai**. §16.1: `StageFollowUpDialog` 346 dòng tách thành `useFollowUpForm` + `FollowUpTeeth` + `FollowUpShots` (còn 177). BE Domain **283** / Application **528** / EF **51** / HttpApi.Host **14** xanh, 0 skip. **Đợt 11 — 2026-09-07 (tối)** (R-274, R-275), hai lệch chủ dự án chỉ ra, soi lại bản gốc **chỉ đọc** (mở dialog rồi đóng, đọc DOM/`getComputedStyle`/React fiber và **bundle** tĩnh; network sau đó xác nhận **không** có POST/PUT/PATCH/DELETE nghiệp vụ nào): nút **Thanh toán** trong "Chi tiết phiếu" nay nhảy đúng phiếu của dòng vừa bấm — `/patient/{id}/treatment-plan/{planId}?planTab=detail&branchId=` — thay vì về danh sách `?tab=treatment-plan` (chip mã DT là đối chứng: cùng trang nhưng **không** kèm `planTab`, nên `planDetailPath` nhận `tab` tuỳ chọn); và **"Danh sách công đoạn" trên tái khám** thôi in `(Trống)` — bundle cho thấy nó **không** phải step của dịch vụ mà là **một** mục sinh từ chính Nội dung điều trị của công đoạn đã hoàn tất (`stageChecklist: a ? [{ id: `${e.id}-re-examination-stage`, label: a, checked: !1 }] : []`), disabled + #2671D8/600 ở dòng listing và **tick được** ở form, còn "Tạo bảo hành" luôn rỗng. Chạy trên bản build production (`vite preview` 8098, API `:5019`, DB thật): 4 spec liên quan xanh, một spec cũ thu hẹp locator vì dòng nay có hai checkbox; bộ `patient` + `treatment-plan` + `treatment-plan-detail` + `treatment-stage` **61 xanh / 2 đỏ**, và cả hai đỏ đã chứng minh là **có sẵn** bằng cách stash toàn bộ thay đổi rồi chạy lại (rác catalog giá 0 của F-39; và spec Tiếp nhận tự tiêu thụ lịch hẹn demo). Việc tick ô checklist có gửi lên server không thì **chưa quan sát được** — `unknowns.md`. **Bổ sung 2026-09-07 (R-283…R-286)**: (a) công đoạn tick `Hoàn thành` thì dòng dịch vụ **rời** tab `TIẾP TỤC CÔNG ĐOẠN` (và khỏi badge), không bị đẩy sang `THÊM CÔNG ĐOẠN`, form đang mở cũng đóng theo; bỏ tick thì về — luật đọc theo công đoạn **live** vì lịch sử chỉ cho tick công đoạn mới nhất; (b) form thêm/tiếp tục công đoạn nay báo lỗi **dưới từng ô** như form tái khám, cả ba ô một lượt, dùng chung `stage/stageFieldErrors.ts` với `useFollowUpForm`. Chạy trên bản build production (`vite preview` 8080, API `:5019`, DB thật): 2 spec mới xanh, cả bộ `patient.spec.ts` **56 xanh / 1 đỏ** — đỏ là spec Tiếp nhận tự tiêu thụ lịch hẹn demo, đã ghi từ R-282, chết ở bước dựng dữ liệu và không thuộc nhánh đợt này. **R-287…R-290 cùng ngày**: bỏ luật "phải có ảnh mới hoàn thành được công đoạn" — luật đó do ta **bịa** (commit `e835c45` tự khai là phỏng đoán), chủ dự án đã kiểm trên bản gốc và `Hoàn thành` tick được khi không có ảnh. Cờ `isImageRequired` vẫn ghi nhận và chỉ hiện thành gợi ý; cái nó thực sự điều khiển là `UNKNOWN_REFERENCE_BEHAVIOR`. 893 test backend xanh, spec mới `Hoàn thành ticks with no image…` xanh 3/3 lượt. **R-291…R-292**: ô `Ghi chú` của "Chi tiết dịch vụ" từng nối `stageNotes.join(", ")` nên dài thêm một đoạn sau mỗi công đoạn — soi lại bản gốc (chỉ đọc) thấy dialog gọi `GET /v1/treatment-services/{id}` và in `note` **của chính dòng dịch vụ**, không phải note của công đoạn; đã đổi sang `line.note`. Bộ `patient` + `treatment-stage`: **60 xanh / 1 đỏ** (vẫn là spec Tiếp nhận tiêu thụ lịch hẹn demo). **2026-09-09 (R-343, R-344)**: cột tiền của hàng phiếu trừ giảm giá hai lần — `payment.totalPrice` mà API trả đã net cả hai tầng chiết khấu, nên `planMoney` đổi sang `total = totalPrice + discount` / `amount = totalPrice`; rollup cũng thôi cộng chiết khấu của dòng đã huỷ (`TreatmentPlan.ServicesDiscountAmount`). Spec mới *a discounted slip prints the gross…* đối chứng bằng cách hoàn nguyên bản sửa (đỏ) rồi áp lại (xanh); `treatment-plan` **6/6** trên cả build production :8080 và dev :5173, Domain **312** / Application **556** xanh. **2026-09-09 (R-345…R-348)**: ba nút hàng có tooltip theo bản gốc (`In bệnh án` · `Hóa đơn` · `Danh sách dịch vụ`; nút `+` không có) qua `ActionTooltip` — bọc `<Tooltip>` trần làm `Esc` đầu tiên đóng tooltip thay vì đóng dialog, nên tooltip bị đóng và giữ khoá ngay ở `pointerdown` capture. Nút bút kẹp nay mở **`PrintMedicalRecordDialog`** dựng theo staging: chọn một trong chín biểu mẫu (mở sẵn *Bìa hồ sơ bệnh án*), zoom 40–120% bước 5% với `Fit` = reset 85% (đo ở ba bề rộng khung — **không** phải fit-width), khung xem trước `#F5F5F5` bo 16, chân `Đóng` / `In bệnh án` in qua portal `.pd-print-sheet`; modal theo **bệnh nhân** chứ không theo phiếu, dùng lại `MedicalRecordSheetView`, sửa ô vàng **không lưu**. Modal **Hóa đơn** đổi 13 ô sang `FloatingLabel` như phần còn lại của source, kèm ba số đo lại (gap cột 20px, tiêu đề 16/700, `Mẫu` để kính lúp ở `prefix`). Kèm hai chỗ lộ ra: `.pmr-scale` tự thu bằng CSS `zoom` vì `--sheet-zoom` chỉ có `.pd-a4-free-page` dùng (tờ bìa ở zoom ≠ 1 bị cắt cụt), và tờ bìa tick sẵn giới tính từ hồ sơ. Real-stack: `treatment-plan` **6/6** và `patient-medical-record` **7/7** trên build production :8080, `treatment-plan` 6/6 trên dev :5173; một đỏ còn lại của `treatment-plan-detail` (`under 640px…`) đã chứng minh là **có sẵn** bằng `git stash` + build lại. |
| F-39 | Chi tiết kế hoạch điều trị (`/patient/:id/treatment-plan/:planId`) | `DIRTY` | `e2e/treatment-plan-detail.spec.ts` (6) | Built 2026-09-07 to **production** (read-only survey, dialogs opened and closed without saving; `docs/clone/pages/treatment-plan-detail.md`). **FE-only** by the owner's decision — every endpoint already existed. Breadcrumb `[mã] - TÊN › Kế hoạch điều trị › mã phiếu`, four grey-pill tabs kept in `planTab`, five head figures (Doanh thu dự kiến · Đã thanh toán · Công nợ · Đã hoàn · Dư nợ). Chi tiết: picker + Thêm công đoạn left, Tạo Đơn Thuốc / In Hóa Đơn / printer right, 15-column table (10 / trang), status pill menu → `complete` / `cancel`. Thanh toán: `Tạo Phiếu Thanh Toán` reuses the shared dialog with the slip's lines pre-ticked; eye and `In hóa đơn tổng` open **"Chi tiết phiếu"** (CHI TIẾT PHIẾU / THÔNG TIN KHÁCH HÀNG / CHI TIẾT DỊCH VỤ / TỔNG THANH TOÁN DỊCH VỤ, `In Hoá Đơn` prints only the receipt). Hoàn tiền: dialog with Loại / Hình thức (three labels only: Tiền mặt / Chuyển khoản / Quẹt thẻ, no account picker — the aggregate's account guard now applies to money coming in only, `PatientPaymentTests` 13/13) / Ngày tạo stacked left, Nội dung 0/500 right, a box per paid line capped at the **net** held (the API's `paidAmount` is already net of refunds — an earlier draft subtracted refunds twice), `Tổng tiền trả`, Lưu → one `POST patient-payments` kind 2 with manual items. Dư nợ: 8 columns, no Ghi chú. ≤640: every table folds into `RecordCard` with the index on the head, shared pager. App primary colour and shared pager instead of the reference blue and Trước/Sau pager. Real-stack on the production build (`vite preview` :8093 this session, API :5000): create slip → code link → page; pay → `Hoàn tất` row, figure moves, receipt dialog, aggregate; refund → row + `Đã hoàn` moves; reload keeps `planTab`; Hoàn thành survives reload; 640 cards; branch-2 refused. **6/6**, `tsc` + lint clean. Limitations (documented in unknowns): `Thêm dịch vụ mới` and `Chuyển đổi` stop at a toast; a refund on a fully-paid line is refused by the server; Dư nợ tab semantics assumed. **Tối cùng ngày** (R-248…R-250): `PrescriptionDialog` tự import CSS (modal đơn thuốc từ trang này từng mất style); dialog Hoàn tiền ≤640 gập thành `RecordCard` đánh số với ô tiền trải hết thẻ (`RecordCardRow.stacked`), ô Nội dung 152px ở một cột; **6/6** trên build production :8097. R-251: tabs + toolbar + bảng bọc trong khối trắng `.pdt-body` (bo 12px, viền `--tp-line`, padding 16px) như bản gốc; **6/6** trên build :8098. **2026-09-07 tối (chưa retest → `DIRTY`)**: `Thêm dịch vụ mới` thêm dòng mới sửa tại chỗ với Lưu/Hủy, pill 7 trạng thái; chế độ nhóm hiện bảng dịch vụ của nhóm trong popover; BE thêm `POST patient-treatments/{id}/services` + migration `20260907090000` (host đang chạy chưa nhận) |

## F-19 note — assumed, not observed

The reference never exposed a treatment-stage payload that could be read without
mutating production. What was observed is only: the ability subject
`treatmentStage` and its six verbs, the per-service "Thêm công đoạn" action,
`stageIds` / `patientStages[]` on CSKH records, and `stageNote` in the treatment
summary. Everything else — sequence numbers, tooth selection, timestamps, and the
image rule — is BlueDental's own design and is documented as such in
`TreatmentStage`.

## Backend regression nets (not acceptance)

| Suite | Count | Last run |
|-------|-------|----------|
| `BlueDental.Domain.Tests` | 196 | 2026-08-26 — pass |
| `BlueDental.Application.Tests` | 474 | 2026-08-26 — pass |
| `BlueDental.EntityFrameworkCore.Tests` | 39 | 2026-08-24 — pass |
| `BlueDental.HttpApi.Host.Tests` | 15 | 2026-08-24 — pass |
| `BlueDental.FE` Vitest | 3 | 2026-08-24 — pass |

## Still not covered

- **Công cụ** (F-16): gọi điện / SMS / Zalo / hoá đơn điện tử — the reference had
  no data to observe.
- **Xuất Excel / PDF**: wired on patients, labo, CSKH, reports, prescriptions,
  treatment plans and invoices. Screens without an export endpoint still have no
  button.
- **i18n export language**: a PDF or Excel export is still generated in
  Vietnamese regardless of the UI language.
- **i18n on the login screen**: the switcher lives in the app header, which only
  exists after sign-in, so the sign-in page is always Vietnamese.
- Two catalogs ("Thẻ hồ sơ", "Phương thức thanh toán") that the reference does not
  model as catalogs.
