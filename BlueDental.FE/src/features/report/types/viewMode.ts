/** Period granularity shared by the report toolbar and report page. */
export type ReportViewMode = "day" | "week" | "month" | "year";

export const REPORT_VIEW_MODES: ReportViewMode[] = ["day", "week", "month", "year"];
